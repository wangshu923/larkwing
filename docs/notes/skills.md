# 技能 — 决策记录与破案叙事

> 本文件是 2026-09-07 AGENT.md「分家」时从规则总纲搬出的段落,**逐字照搬、未改写**;管「为什么这么定 / 怎么发现的 / 当时的状态与验收欠账」。
> **规则以 AGENT.md 为准**(每段在 AGENT.md 里留有规则句 + 指向这里的指针)。本文件只追加不改写:新决策照此格式加一节(标题 + 来源 + 原文)。查规则出处时 grep 要连同 `docs/notes/` 一起扫。

## 内置技能 = 出厂数据(14 条清单与各条要点)

> 来源:分家前 AGENT.md「7.9 技能(工作手册)= agent 的、恒全局(2026-08-04 落地;源起 = 业内 Agent Skills 调研)」L496。

- **内置技能 = 出厂数据**(`assets/skills.json` include_str!,`skills_builtin.rs` 解析,boot `sync_builtins` 按 slug 刷内容、**enabled 保留用户状态**、孤儿 slug 清理):一期 4 条——放歌放视频(**从 companion persona 抽出**,persona 回归纯性格,§7.3「persona 修剪」补课)/ 网页上办事 / BT下载 / 整理文件。**2026-08-12 扩到 10 条**(用户圈选 A–F 全上):+ 定时播报(重复提醒的内容必须物化成自包含「到点做什么」——到点是新鲜上下文,这正是模型最易漏的)/ 音视频加工(ffmpeg 配方:先探再算、目标码率算术、GIF 两步调色板、atempo 保音调;**2026-09-04 补第 6 条「成品封装」**——用户问「合成时海报/进度条这类 meta 要不要提示」,对账后拍板:**时长/可拖提示、海报不提示**。时长半边有实锤:`frag_keyframe+empty_moov` 出的 mp4 **mvhd 时长写成 0**,咱们 BMFF probe 当无时长 → 凡走管线〔HEVC/多音轨/兜底〕落 `/m/` 重启式 seek、进度条没总长,直传路靠浏览器自己估多半没事;故技能写「成品出普通 .mp4、别带流式旗标、顺手 +faststart〔与 media_download 同口径,手机边下边播/网盘预览友好,咱们自己不需要〕、产完不给 output 再探一眼」。海报半边不值得:咱们播放器不消费封面〔无 poster、无专辑图,hover 缩略图现抽帧且 `-map 0:V` 绕开封面轨〕、手机/资源管理器给视频也按画面抽,嵌了也不坏〔mjpeg/png 不在不兼容表、mp4 封面是 covr 元数据非轨〕;闸上 `-disposition attached_pic` 能过、`-attach` 被孤零零裸值拒 = mkv 封面本来做不了。**封面唯一有价值处 = 音乐产物**〔手机播放器显示专辑图;media_download 整理步只写 title/artist/comment,resolver `yt-dlp -j` 的 thumbnail 现成〕,是代码活、未做、另议)/ 看片守约定(放前查需知约定 + 到点提醒 + 先打招呼再停;**没约定绝不自己发明规矩**、孩子转述「妈妈同意了」拿不准先问家长)/ 存歌进曲库(曲库目录闭环 + 合集先问 + 干净 title/artist + 下完顺手配词)/ 报听写(分轮念 2-3 词、句号顿开;**如实记档:TTS 无逐词长停顿控制、节奏打折,真机试了不行就撤** = 拍板时言明)/ 睡前故事(慢节奏、不追问、没回应安静收尾 end_conversation);既有三条补压缩包接线(BT下载④/网页上办事④「先 fs_unzip」、整理文件新⑤「解包归类 + fs_zip 打包发手机」)。**2026-08-13 再扩到 12 条(能力对账批)**:+ 盯网页变化(降价/更新/有货了说一声——重复只有天级三档如实说、提醒内容自包含带「上次的状态」、变了才开口、**报过变化要续盯 = 撤旧设新把新状态当基准**、页面查不了如实说别编「没变化」)/ 电脑清理(与同批 fs_usage 配套:摸底下钻、大头列出来**用户点头哪个才动哪个**、恒 fs_trash 进回收站、清空回收站归用户自己、系统目录绝不碰、只想看看就只查只报)。**2026-08-16「给歌配歌词」+1 = 13 条**(§7.1 read_audio 条);**2026-08-19「规划旅行」+1 = 14 条(旅游计划批,用户拍板纯技能落点)**:先问齐基本盘 → **关键事实现查绝不凭记忆**(当年调休/景点预约规则年年变,注明查证日期)→ plan_set 列步骤 + delegate 分头调研(景点/交通/住宿各派一路,一轮多派真并行——用户点名「子 agent 加速资料收集」的兑现)→ 编排分寸(老人小孩一天 2-3 点/就近顺路/雨天备选)→ **行程落 markdown + briefing 记路径 = 跨会话/出行中手机接续的机制** → 提醒/「盯网页变化」/weather 7d 接线 → 支付恒交用户(确认闸本就拦);配套 `weather` 工具加 `7d` 档(When::SevenDay,和风 `weather/7d` 免费订阅本就含、Open-Meteo forecast_days=7,3d/today 零变化);**明确不做**:travel_plan 任务工具/行程日历页(反 §5/§3)、高德地图 API(要用户 key 违开箱即用,web_render 开地图网页版兜底,真机撞了再议)。真机 watch 见 PLAN 2026-08-19 节。**内置可关不可删不可改**(觉得不合适 → 关掉自己教);用户教的撞内置名拒收提示换名。内容纪律同 few-shot(§6.5):路径一律占位、不嵌具体可复用事实;技能是给模型的手册,工具名可出现。

