# LLM 供应商 / 目录 / 密钥 — 决策记录与破案叙事

> 本文件是 2026-09-07 AGENT.md「分家」时从规则总纲搬出的段落,**逐字照搬、未改写**;管「为什么这么定 / 怎么发现的 / 当时的状态与验收欠账」。
> **规则以 AGENT.md 为准**(每段在 AGENT.md 里留有规则句 + 指向这里的指针)。本文件只追加不改写:新决策照此格式加一节(标题 + 来源 + 原文)。查规则出处时 grep 要连同 `docs/notes/` 一起扫。

## 中立 ChatMessage 终态 + 工具结果多媒体 + 丢图必留话

> 来源:分家前 AGENT.md「6.3 llm」L184。

- **中立 `ChatMessage` 终态**(纸面已定,分期抵达):`User{content, parts} / Assistant{content, reasoning, reasoning_state, tool_calls} / ToolResult{call_id, content, parts}`,`ContentPart`(Text/ImageUrl;Json 尚未加)。厂商 JSON 永不出 provider 文件。接触面锁死在 engine(唯一构造点)+ 各 provider `to_wire()`,store/IPC/前端不依赖它 → 重构成本不随 app 长大。**工具结果多媒体已落地(2026-07-14,commit 68419ae)**:`ToolResult` 加 `parts`(空则出向字节不变、缓存零回归)+ `Tool::run_output`(默认包 `run` 的纯文本,39 工具零改,要回图的工具覆写它)+ 四供应商出向各自处理图(Anthropic tool_result 内嵌 image block、Gemini functionResponse 旁挂 inlineData、OpenAI Responses function_call_output 带 input_image、**chat-completions 协议不容 tool 消息带图 → 降级纯文本**);非视觉模型(catalog `vision`)一律降级;图从不落库/不回放(§9,同用户发图当轮注入)。**丢图必留话(2026-07-15 修 bug,用户拍板)**:非视觉 / 协议不容而丢弃 ToolResult 图片时,四方言共用单源 `llm::tool_result_text` 在结果文本追加「附带的 N 张图片没能传给当前模型…」——此前是**无声丢弃**,而 web_render 文本宣称「(已附上当前页面截图)」= 给非视觉主脑喂假前提(模型会装看过画面);无图 / 已送达字节不变、缓存零损伤。**消费者两个:web_render 截图(推送式)+ `read_image`(拉取式,2026-07-15,§7.8)**。只单测验出向 JSON,真 API 行为(视觉模型真看到工具图)真机待验。

## 图片按 catalog 视觉能力降级 / vision override / DeepSeek 视觉模型 / 工具图转交

> 来源:分家前 AGENT.md「6.3 llm」L190。

- **图片按 catalog 视觉能力降级(2026-07-04 真机实锤)**:非视觉模型(DeepSeek 全系)收到 `image_url` 直接 API 400(`unknown variant image_url`)→ `catalog::ModelInfo` 加 `vision` 列(数据;未知模型按 false,宁可少看图不可打挂回合),`openai_compat::to_wire` 出向时把图降级成占位文本,回合不炸;Gemini/Claude/GPT-5 视觉系原样带图。**(2026-07-15 补)三件**:① **「服务商会不会自己占位」不可赌,降级必须客户端收口**——同一家不同端点行为都不一(DeepSeek OpenAI 端点硬 400,其 Anthropic 兼容层却占位成 "[Unsupported Image]",robot 记档);② **vision 进「高级设置」override**(`ModelOverride.vision` 三态,None=目录;自架 llava / qwen-vl 类靠它标上、目录说错也靠它纠;`supports_vision` 先查覆盖再查目录),UI = 大脑卡高级区「图片理解」SkinSelect(自动/能看图/只看文字);③ **目录 2026-07 校订**(用户报截图对账 + 官方核实):Kimi K2.5/K2.6 原生多模态(官方 API,特异行在 kimi-k2 之前防错杀)、Qwen 补 qwen-vl / qwen3-vl / qwen-omni / qwen3-omni 视觉家族行(qwen-max 的 API 仍纯文本)、DeepSeek API 仍无图片输入(网页端灰度不算,目录不动)。桌面发图与手机 TG 发图同一条链、同修。⚠️ **占位文本必须引导「用工具处理刚发的图」,不能只说「看不了」(2026-07-10 真机实锤弯路)**:只说看不了 → 模型放弃这张图、转而要用户报文件路径(发图入口白设)。**关键洞察:认二维码/条码、图转格式这类与视觉无关**——有眼睛也读不出码里的 URL(纠错编码位图,得算法解码),永远走工具;故非视觉占位明说「你有工具能直接处理最近发的图(认码/转格式/读文档),意图对得上就调,别让用户报路径;只有要描述画面才如实说看不了」。`qr_decode` 等缺省参数本就吃「最近一批图」(§7.8),占位打通入口后模型不再要路径。**(2026-09-03 增补)DeepSeek 上线官方视觉模型,上句「DeepSeek API 仍无图片输入」自此只对 flash/pro 成立**:2026-08-21 DeepSeek 调整产品线为 `deepseek-v4-flash` / `deepseek-v4-pro` / **`deepseek-v4-flash-vision-exp`**(实验性多模态:文本能力 = flash、图 ≤384 token 计费与 flash 一致、标准 OpenAI `image_url` 收 base64 data URL / http URL;同批引入分时计价,空闲时段对折)。目录改三处:① 加 vision-exp 行(vision=true、Light、1M)——**它的 id 包含 `deepseek-v4-flash` 子串,必须排在 flash 行之前**,否则子串匹配被 flash 行(vision=false)抢先命中、图遭降级 = 用户手填视觉模型也白配(kimi-k2.5 被 kimi-k2 错杀同款坑;测试钉着,失败即排序被动过);② flash / pro 牌价刷成 2026-08-21 高峰价快照($0.44/$1.32、$1.32/$3.96;空闲对折不建模,同缓存折扣「宁可高估」口径);③ 出向、thinking quirk、前端、默认模型 v4-pro **全部零改**(咱们发图本就是 data URL,`openai_compat` 既有 vision 分支直通;`-exp` 实验模型不宜做默认)。**边界如实**:用户把 DeepSeek 模型改成 vision-exp 后,桌面/手机**发图能看**了(默认供应商第一次有看图能力);但 DeepSeek 明文「图只许出现在 user 消息,system/assistant 带图 400」→ ToolResult 图(read_image / web_render 截图)在 chat-completions 路仍按本条既有规则降级留话——打通 = 「工具图经下一条 user 轮转交」quirk 或 §7.8 视觉子调用,****2026-09-04 用户拍板已做**:`openai_compat::to_wire` 对 `catalog::supports_vision(model)` 为真的模型,ToolResult 文本改用 `tool_result_text(.., delivered=true)`(不再留丢图话),把这一轮所有 tool 消息的图攒进 `pending_tool_images`,在这组连续 tool 消息之后、下一条非 tool 消息之前(或消息尾)`flush_tool_images` 追加**一条** `role:user` 消息 = 中性说明 text part(「转交:上面工具结果附带的 N 张图片,供你查看;这不是用户发言」)+ 各 image_url part;非视觉模型 / 无图 ToolResult 字节不变,多轮各自 flush;只改出向序列化,engine / store / 其余三方言零改(它们本就原生带图)。**判据 = 图仍不落库不回放**,转交只发生在工具跑的那一轮。真 key watch:DeepSeek vision-exp 是否接受 tool 消息后紧跟带图 user(PLAN 2026-09-04·二批);2026-09-03 节的其余 watch 不变。

## 模型清单现查服务商 / 厂商预设 / 思考方言三位

> 来源:分家前 AGENT.md「6.3 llm」L191。

- **模型清单现查服务商、不写死名单(2026-09-04;设置·大脑「模型」框的下拉)**:用户要「下拉快捷选模型」→ 落法 = `LlmProvider::list_models`(trait 新方法,默认空清单)点 ▾ 时**向该供应商接入点拉「这把钥匙能用的模型」**:OpenAI 系 / DeepSeek / Ollama `/v1` GET `/models`、Anthropic GET `/v1/models?limit=1000`、Gemini GET `/models?pageSize=1000` 只留 `supportedGenerationMethods` 含 generateContent 的(靶文档化字段不靶名字);解析单源 `llm::parse_openai_models`(三家同形)+ gemini 私有 parse;全走 `net::Client`(§4.6)。**为什么不写一份静态名单**:DeepSeek 两个月换三次产品线、中转 / 自架的名字千变万化,静态名单 = 必陈的硬编码(§4.11 同族);真相源在服务商,**目录只负责贴标签(档位 / 能看图 / 牌价,覆盖优先)+ 排序(目录认识的靶前、组内保服务端原序,不认识的沉底不删——不按名字筛「能不能聊天」,embedding/tts 之类照列、自然沉底)**。engine `list_models(provider_id)` → `ModelChoice{id,known,tier,vision,in/out 价}`;命令 `list_models` 是 **async**(§8.4);牌价取用口收口 `catalog::prices_of`(est_cost_usd 同源)。**前端 = combobox 不是 select**:输入框保自由文本(中转模型名随便填),行尾 ▾ 开清单、按供应商缓存、边打边筛、↑↓ 回车、Esc 撤销筛选、没钥匙 / 401 / 连不上 / 端点不实现各给一句人话(kind 映射 i18n,§6.6)、清单空或没匹配也明说「手填也行」(§3.5)。**两个前端坑(实锤)**:① **`:value` 单向绑定的输入框,一旦组件每键重渲染就会被 Vue 钉回旧值**(runtime-dom patch `value` 时 DOM 值 ≠ 绑定值就覆写)——接入点框没事只因打字不触发重渲染;模型框要筛清单必重渲染 → 必须绑「草稿 ?? 已存值」(`modelDraft`),草稿同时就是筛选词,存成了再退场;② **设置页在 `window` 上听 Esc 关整页**,下拉内 Esc 要 `stopPropagation`(SkinSelect 的 Esc 同一隐患,记档未改);另:行 / ▾ 按钮 `pointerdown.prevent` 保住输入框焦点,免得点选时 blur 先把半截文字当模型存上。自定义卡(未接入的那张)不带下拉(没 provider id 可查);真网 `#[ignore] real_deepseek_list_models` 探针 = 「DeepSeek 又换名单」的绊线;真机 watch 见 PLAN 2026-09-04 节。**同日二批:厂商预设 = 数据、不带默认模型**(用户拍板「主流就行 / 不写死」):`registry::presets()` 一张表 —— 千问 / 豆包 / Kimi / 智谱 / 混元 五家国内主流 + 已有但 UI 原先够不着的 OpenAI / Gemini / Ollama(后三家接入点取自 `LlmConfig` 预设构造器,单源不抄字面量);字段 = id / 名字(品牌专有名词,不进 i18n)/ 协议 / 接入点 / 钥匙占位(Ollama 本地用占位放行 `usable()`),**刻意没有默认模型字段**。入口 = 「自己接一个大脑」卡**接入点框自己的 ▾**(与模型框同手感;首版做成卡顶单独一行「预设」下拉,用户拿着截图问「接入点怎么触发快捷选择」= 人到那个框上找,当天挪回框上、砍掉那一行):选一家自动填名字 / 协议 / 接入点(手改地址即摘掉 ✓),贴钥匙后模型框 ▾ 走新命令 `list_models_draft(protocol, base_url, api_key)` 对**还没接入的草稿配置**现拉、选好再接入(钥匙是前端→后端方向,与保存同向,不违「凭证不过桥」;engine `list_models_for(spec)` 已接入卡与草稿共用一条路);协议由两键 seg 改成四选 SkinSelect(Gemini / OpenAI Responses 原生路首次有 UI 入口,徽章文案 `settings.brain.native`)。卡片列表不动、仍只常显 DeepSeek / Anthropic 两张模板(它们不进预设表,免同一家两个入口;§3 收敛)。弹层抽成 `components/ModelPickList.vue` 两处共用(`.link` 等样式在组件内复刻——SettingsView 的 scoped 样式进不来,§6.7)。**「有没有列模型端点」的定案手法**:五家官方文档都没写,拿假钥匙打 `/models` 与一个乱路径做两组对照 —— 401 / 404 = 端点在(千问 / Kimi / 混元 / MiniMax / 硅基流动),401 / 401 = 网关先验钥匙再路由、证不出(豆包 / 智谱,待真 key)。目录顺手 +`kimi-k3` 行(Smart / 能看图 / 1M,价留 None)。**撞名教训**:同文件早有 `applyPreset`(性格快捷 chip),新函数落地前先 grep 同名——模块级重复 `function` 声明是语法错,整页白屏而 vue-tsc 才报 TS2393。**(2026-09-04·二批)五家真正接通 = 目录 + 思考方言 + 混元接入点(用户拍板)**:① **目录**加豆包(`doubao-seed-2-1-pro`/`2.1-pro` Smart·能看图·256K,`2-0-lite` Balanced、`2-0-mini` Light,宽泛 `doubao-seed-2`,`1-6`/`1.6`;**火山方舟 ID 把点写成横杠,两形各一行**)、智谱(`glm-5v-turbo` 能看图·200K 排在 `glm-5` 前 / `glm-5-turbo` / `glm-5.3-flash` 排 `glm-5.3` 前 / `glm-5.3` Smart·1M / `glm-4.5v`·`glm-4v` 能看图)、混元(`hy4-preview` Smart·1M / `hy3` Balanced·256K / `hunyuan-vision` 族)行,Kimi 补 `k2.7-code`·`k2.6` 能看图 + 价、千问补 `qwen3.8-max` 能看图·1M 等 —— **没有这些行,预设五家的视觉模型发图会被 `openai_compat` 悄悄降级成占位文本**(vision-exp 错杀同类)。人民币牌价经单源 `catalog::CNY_PER_USD = 7.0` 换算(2026-09 汇率快照取偏低值让估价偏高;**§4.11 待用户确认**);豆包 2.1-pro / glm-5.3 牌价来自转述标「待核」。② **思考方言拆三位**(`Quirks`):`thinking_toggle`(带 `thinking:{type}`)/ `reasoning_roundtrip`(带 tool_calls 的 assistant 轮回传 `reasoning_content`)/ `enable_thinking_bool`(千问:非 Off 带 `enable_thinking:true`,Off **不带字段**免得对不支持思考的模型报错);老 `thinking_field` = 前两者合一(DeepSeek 不动,老 JSON 兼容)。**派生单源 = 预设表** `registry::presets()` 各带 quirks(Kimi/混元 = toggle+roundtrip〔两家官方文档都要求工具循环回传 reasoning_content〕,智谱/豆包 = toggle〔回传是否必需未核,不发未知字段〕,千问 = enable_thinking_bool),`quirks_for_base_url` 按接入点主机匹配(`HOST_ALIASES` 把混元老域名映到同套),`ProviderSpec::to_config` 在 quirks 仍默认时派生、显式设过不覆盖;中转 URL 匹配不到 = 现状(什么都不带)。**绑机制不绑模型名**:Kimi k3 始终思考无 thinking 参数、k2.7-code 传 disabled 报错,这类按模型的差异**不加分支**,交真机 watch。③ **混元预设接入点换 TokenHub** `https://tokenhub.tencentmaas.com/v1`(官方文档现只写它,hy3/hy4 在这;老域名 `api.hunyuan.cloud.tencent.com` 仍活着但新模型不在那,quirks 派生两个域名都认)。真 key watch 见 PLAN 2026-09-04·二批。

## 密钥落 keyring(仅 Windows 启用)

> 来源:分家前 AGENT.md「6.3 llm」L194。

- **密钥落 keyring 已落地**(2026-06-17,§6.3 原「发布前换 keyring」待办兑现):`secrets` 模块(`secrets.rs`)把秘密类 key 存系统密钥串,**不进 SQLite 明文**。**keyring 仅 Windows 启用**(目标平台,凭据管理器随登录会话解锁、不弹框);**mac / Linux 开发机回落 `settings`**(明文,dev 可接受 §4.9)——mac Keychain 对未签名/每次重编的 dev 二进制会弹「允许访问钥匙串」,太烦(2026-06-17 用户拍板去掉),gate 在 `secrets::entry()`(非 Windows 返回 None)。秘密清单 `SECRET_KEYS` = `llm.api_key` / `llm.providers`(整块,含各 provider key)/ `crypto.ed25519.private_key` / `remote.{telegram.token,dingtalk.app_key,dingtalk.app_secret}`;**Ed25519 公钥不在内**(非秘密、给用户复制)。读写一律走 `secrets::get/set`(engine 的 load_registry/set_api_key/persist_specs/ensure_app_keypair、weather 签 JWT、channels 凭证、set_setting 的 `remote.*` 秘密臂、`remote_status` 的 configured 检查全已路由)。**keyring 不可用(headless/dev/无后端)→ 回落 `settings` 并 warn,绝不让 app 哑掉**;存的是原文(literal 或 `${ENV}`),`resolve_env` 在取值时跑。boot 调 `secrets::migrate` 把 legacy 明文一次性迁入(幂等)。✅ 2026-06-30 Windows 真机验过(凭据管理器存取 + legacy 明文迁移)。

