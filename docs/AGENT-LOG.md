# AGENT.md 整编日志(历次会话的规则变更纪要)

> 2026-09-07 从 AGENT.md 页脚搬出(逐字)。它是「哪次会话改了哪条规则」的链条,曾用来对 git log 查漏回写;以后每次整编在**本节之前**加一段(最新在上),AGENT.md 页脚只留最近一次的一行。

## 2026-09-07·三批 AGENT.md 分家

叙事 / 状态 / 整编链搬出规则总纲:每条规则原位留「规则句 + 落点 + 指针」,原文逐字进 `docs/notes/<主题>.md`,页脚整编链进本文件,§0「现状」段归档在下方;新增 §10「体量纪律」+ 文档地图两行。

## 分家前的整编链(2026-06 → 2026-09-07,原页脚一段,逐字)

*最后整编:2026-09-07·二批(**音乐播放改版**:用户提四条〔上下曲等快捷键系统化 / 海报 / 循环随机合一 / 列表钮〕→ 方案讨论拍板〔三档〔默认列表循环,顺序档砍掉〕· N/P 换曲不占 PageUp/Down · R 模式 C 歌词 · 内嵌封面走 ffmpeg 不引 lofty〔MSRV 撞墙:lofty 0.25 要 1.89、id3 1.17 要 1.85〕· 侧车名单 cover/folder/front/album · 512/q85/16 · 下载嵌封面原图不裁 · 「正在播放」大卡一起做〕→ 当日落地:core `PlayMode` 三档 + `MediaEvent::Mode` + `NowPlaying.play_mode·cover_url·album` / relay `/cover/{token}` 三源 + `cover_jpeg` / `Resolved.thumbnail` / probe `attached_pic·tag_album` / 下载 `remux_args` 嵌封面 / 前端 `useMediaKeys` 两面共用 + `KeyHelpCard` + 播放条改版〔封面 · 模式钮 · 焦点描边 · OSD 药丸 · 大卡三行歌词〕+ 悬浮窗封面 + Media Session artwork;规则变更 = §7.1 新 ★ 条 + 三处订正 + §4.11 清单;Mac 全绿 lib 807 / clippy 触点零新警 / cargo check --workspace / vue-tsc 0 / i18n 镜像 884=884 / 预览端到端验过;真机 watch 见 PLAN「2026-09-07·二批」)。前次 2026-09-07(**播放体验五批**:用户提六条〔上下集快捷键 / 片头片尾 / 1.5 倍切集掉回 1.0 / 多集列表按钮 / 倍速声音优化是啥 / 倍速 1.5–2.5 加密〕+ 追加「昨晚看到 4 集今天回到 2 集」→ 三轮讨论拍板〔H 帮助键 · Ctrl+←→ 倍速 · ←→ 15s / Shift 60s · 十档 0.5–3 · 片尾 3 秒倒计时可取消 · 进度按家记 · 电影也记 · 集内 30s 落盘 · 片头片尾「先元数据再指纹,行为学先算了」· 按剧存太粗 → 从本集起规则〕→ 五批当日落地:粘住 + 档位菜单 / 续播重做〔迁移 0032 按家记 + 集内秒数 + episode 参数 + 决策日志 + Series 剧名〕/ 快捷键表 + OSD + Media Session / 剧集列表 / 片头片尾〔media_skip + skip::resolve 四路 + 章节解析 + B 站 clip_info〔后台 agent 实测调研〕+ introdetect 指纹〔后台 agent 在 worktree 写的 fingerprint 引擎合入〕〕。规则变更 = §7.1 续播规则重写 + 粘住口径重写 + 新 ★ 条,§4.11 双源清单追加(三闸 / 检测常量 / 前端判定待用户确认);Mac 全绿 lib 800 / clippy 触点零新警 / cargo check --workspace / vue-tsc 0 / i18n 镜像 927=927 / 预览端到端验过;真机 watch 见 PLAN 2026-09-07 节)。前次 2026-09-04·四批(**聊天流「没消息却一直往下滚」bug 修 = 桌宠移出滚动容器**,§8.6 新陷阱条:用户真机视频 → 视频抽帧量几何 + 预览合成同构测试件逐像素挪定案「transform 定位盒算进 scrollable overflow + `.body` 并非零尺寸」,与「贴底滚动」成正反馈环;修 = MainLayout `.stream-wrap` 同框外层 + PetRoamer 直写视口坐标;预览 1:1 复现〔24 条增量 scrollTop 663→844〕→ 修后纹丝不动、几何零偏移、拖拽落点正确;vue-tsc 0;CHANGELOG 0.2.35 节是别会话 WIP 未动;Windows 真机 watch 见 PLAN 同日节。规则变更 = §8.6 新增,过程级)。前次 2026-09-04·三批(**「音视频加工」技能补第 6 条「成品封装」**,§7.9 记档:用户问「有内置视频技能吗?合成时海报/进度条这类 meta 要不要提示」→ 对账 = 有 av-recipes(五条全配方、无成品 meta);拍板 **时长/可拖提示、海报不提示**。时长半边本机 ffmpeg 8.0 实测:普通/faststart mvhd=4.0,`frag_keyframe+empty_moov` mvhd=**0**,仅 frag_keyframe 短片 4.02〔长片只记首块〕→ 咱们 BMFF probe 当无时长,走管线的落 `/m/` 进度条没总长;海报半边:播放器不消费封面、嵌了也不坏、`-attach` 本就过不了闸,唯一有价值处 = 音乐封面〔media_download 整理步不嵌,thumbnail 现成〕= 代码活未做另议。纯数据改动,零代码零 i18n,skills 10 测全绿;技能行「产完探一眼」是可选项。真机 watch = 模型是否照第 6 条办)。前次 2026-09-04·二批(**「换脑更顺手 + 眼耳补齐」六件**:用户「这个版本有点单薄,看看还有什么值得做的」→ 按代码对账荐 4 主推 + 4 小件 → 拍板 1 五家接通 / 2 工具图转交 / 4 复审 + FTP 断点续传 + B 站缩略图 + 声音明信片,**否决「视频消息」**(「看视频没啥用」,候选池官方缺口清零);「分头行动」= 五路 agent 各在独立 worktree 并行落地〔两轮撞用户会话额度全灭,半成品 wip 提交到旧分支、新 agent 首步 checkout 续跑;brief 里「AGENT.md 绝不整读、grep 定位再局部 Read」是省额度关键〕。落地:§6.3 国内五家真正接通〔豆包/智谱/混元入目录 + Kimi·千问补行,`Quirks` 拆 thinking_toggle / reasoning_roundtrip / enable_thinking_bool 按预设表派生,混元预设换 TokenHub〕+ 工具图经紧随 user 消息转交给能看图的 chat-completions 模型;§7.8 FTP `REST` 续传〔假 tokio FTP 服务器四条端到端〕+ `speak_to_file` 声音明信片〔四件套 + delegate 保留集〕;§7.1 B 站进度条雪碧图缩略图〔videoshot 免 WBI、`index` 首项哨兵实锤、与 yt-dlp 解析并行 3s 兜底、前端零改〕;§7.10 复审 0.2.32 后新代码零 Critical 修 9 处〔头号 = 按内容嗅探图片不能用带扩展名提示的 `ImageReader::open`;Windows-only 改动全过 §8.5 靶场〕。Mac 全绿 lib 781 / clippy 触点零新警 / cargo check --workspace / vue-tsc 0 / i18n 镜像 830=830;**§4.11 待用户确认常量五组**(CNY_PER_USD=7.0 · FTP 续传 5 次+退避 · speak_to_file 1000 字+默认名「留言」· B 站雪碧图 3s/16 张/8MB/q80/8192)与真 key / 真机验收单见 PLAN「★ 2026-09-04·二批」;CHANGELOG 已起 0.2.35 节,版本号未 bump、未 tag)。前次 2026-09-04(**设置·大脑模型下拉 = 向接入点现查 `/models`**,§6.3 新条:用户看着大脑卡截图问「模型能不能做下拉之类的快捷选择」→ 拍板方向 = **不写死名单**(刚被 DeepSeek 两个月换三次名单教育过;静态名单 = 必陈硬编码 §4.11 同族),点 ▾ 向该供应商接入点拉「这把钥匙能用的模型」,目录只贴标签 + 排序。落地 = `LlmProvider::list_models` trait 方法(默认空)+ 四协议各自端点(OpenAI 系 `/models` / Anthropic `/v1/models?limit=1000` / Gemini `/models?pageSize=1000` 按 supportedGenerationMethods 筛)+ 解析单源 `parse_openai_models` / `status_error` + `catalog::prices_of` 取价单源(est_cost_usd 同源)+ engine `list_models` → `ModelChoice` 认识的靶前 + async 命令 `list_models`(§8.4)+ 前端 combobox(自由文本保留、按供应商缓存、边打边筛、↑↓回车、Esc 撤销、错误按 kind 给人话、空 / 没匹配明说手填也行)+ i18n 9 键双语。**预览实测抓到两坑并修**:① `:value` 单向绑定 + 每键重渲染 → Vue 把 DOM 值钉回旧值(打的字被覆盖、筛选却生效)→ 改绑草稿 ?? 已存值;② 下拉内 Esc 冒泡到 window 把整个设置页关了 → stopPropagation。Mac 全绿 lib 753〔新增 3 测 + 1 真网 #[ignore] 探针〕/ 工作区 check / clippy 触点零警〔顺手修 mod.rs · catalog.rs 两条存量 doc 缩进警〕/ vue-tsc 0 / i18n 镜像 824=824 / 预览端到端验过(拉清单 / 标签 / 筛选 / 点选落值 / 无钥匙提示 / Esc 还原 / 键盘选)+ 零 console 错;**真网未验**(本 shell 无 DEEPSEEK_API_KEY,探针留好;四家真端点 + 中转 / Ollama 行为见 PLAN 2026-09-04 watch)。**同日二批 = 厂商预设**(用户看完端点矩阵拍板「主流就行 / +几张预设也行 / 不写死」→ 预设表进 Rust `registry::presets()`〔千问 / 豆包 / Kimi / 智谱 / 混元 + 已有的 OpenAI / Gemini / Ollama〕、**无默认模型字段**;「自己接一个大脑」卡的接入点框 ▾ 列预设自动填名字 / 协议 / 接入点〔首版单独一行「预设」下拉,用户实锤「到框上找」当天挪回〕,模型框 ▾ 走新命令 `list_models_draft` 对未接入配置现拉、选好再接入;弹层样式上全局 `.pick-*`(两处共用);协议改四选 SkinSelect〔Gemini / Responses 原生路首次可从 UI 接入〕;弹层抽 `ModelPickList.vue` 两处共用;各家「有没有 `/models`」不靠文档猜,假钥匙打 `/models` vs 乱路径两组对照定案〔千问 / Kimi / 混元有,豆包 / 智谱网关先验钥匙、待真 key〕;目录 +kimi-k3 行。Mac 全绿 lib 754〔+presets 测〕/ 工作区 check / clippy 触点零警 / vue-tsc 0 / i18n 镜像 829=829 / 预览端到端验过〔选预设自动填 → 无钥匙提示 → 填钥匙拉清单 → 点选落值〕;撞名 `applyPreset`〔性格 chip 同名,模块级重复声明整页白屏〕→ 重命名 `applyProviderPreset`,教训进 §6.3。五家真 key 拉清单 + 豆包 Model ID / ep 两形 = 真机 watch)。规则变更 = §6.3 新条(含二批增补),过程级)。前次 2026-09-03(**DeepSeek 官方视觉模型入目录 + 牌价刷新**,§6.3 图片降级条增补:用户看 DeepSeek 定价页问「上了 flash 和支持视觉的 flash,咱们要改什么」→ 官方文档对账(定价页 zh/en + 图像理解指南 + 2026-08-21 公告)+ 代码对账后定案 = 只需目录数据一批小改,但其中一条不改就真出错:`deepseek-v4-flash-vision-exp` 含 `deepseek-v4-flash` 子串,现有 flash 行(vision=false)会抢先命中 → 用户手填视觉模型也照样被降级占位。落地 = catalog 加 vision-exp 行排 flash 之前(vision=true / Light / $0.44·$1.32 / 1M)+ flash·pro 牌价刷成高峰价快照(分时空闲对折不建模)+ 测试钉排序与 vision 三态(含中转前缀形)+ 顺手修 catalog.rs 存量 doc 缩进警;出向 / quirk / 前端 / 默认模型零改。**边界如实记档**:配 vision-exp 后用户发图能看,ToolResult 图(read_image / 截图)在 chat-completions 路仍降级留话(DeepSeek 明文图只许在 user 消息)——工具图转交 / 视觉子调用另案未拍板。Mac 全绿 lib 750 / clippy 触点零警;CHANGELOG 未记(v0.2.34 已 tag,随下版节走);真机 watch 见 PLAN 2026-09-03 节。规则变更 = §6.3 记档增补 + 陈说法订正,过程级)。前次 2026-08-31·三批(**桌宠戏份 B1 = 行为层**,纯前端观感、零新美术、零后端零 i18n,2026-08-13·四批「桌宠戏份 A 层」的续篇:用户要「B 层动作帧的炫方案」→ 方案拆两半(B1 行为戏先让它活 / B2 动作帧出图换真姿势),拍板「先 B1 看看」。六条行为全落:睡觉(空闲 3min 就近角落趴下 ≤120px + 呼吸 + Zzz,动静即醒伸懒腰)/ 思考踱步(±40px 小碎步)/ 完工蹦跳撒星星·失败问号蔫(tasks running→done/failed 边沿)/ 送信(ConversationActivity → 叼信封小跑左下)/ 围观打字(输入框内容在变 → 小跑到底边蹲下歪头,停手 5s 走开)/ 拖拽物理 + 戳(pointer-events 只开本体 ~60px:拎起随甩速摆 ±24°、松手抛物线落地压扁回弹;快点 = 惊跳 +「!」)。机制:决策单源 `usePetBehavior`(信号→行为;计时全 setTimeout 零 interval)+ PetRoamer 执行(目标点策略;有目的地的走路加速:送信 1.8>围观 1.6>找觉睡 1.4>闲逛 1>踱步 0.6);一次性姿势走 **WAAPI el.animate**(class 不重播、连点有反应),持续姿态走 class;PetProp 改按 glyph 渲染 + 新增 zzz/bang/question/mail 线稿件(token 换肤);悬浮窗 orb 经 `glyphOf` 映射零变化;B2 挂点已留(同一套行为换真帧)。**验证手法记档(可复用)**:Browser pane 隐藏 = 浏览器冻 rAF + 强节流 timer → 劫持 rAF 手动泵帧 + 合成 pointerdown 骗 usePageVisible + **Vue flush 用微任务等**(同步采样读未 flush 旧 DOM = 本批两次假阴性的根);12 段泵采六行为四戏份特征全对上 + 合成指针三连(bang/grabbed+rotate/落地 y===floor)全过、零 console 错、vue-tsc 零错。明确不做:视线跟随/多宠/节日装扮/桌宠说话/脚印。**同日两补(用户真机首测反馈)**:① 围观打字缓释——原「发送清空即退场」让短消息它才走半路就回头等于白演,改成停手/发送后再蹲 5s 才走;② **悬浮窗同步**(用户点破「悬浮窗没有同步吗」——主窗常年藏托盘,悬浮窗才是天天看到的脸):状态类行为映射到 orb(打盹 = img 变暗+慢呼吸+Zzz 角标、完工 = 蹦两下+星星、失败 = 问号+蔫、送信 = 信封角标;跑动类没戏台不映射),WAAPI 打在 img 上(.orb 的 translateY(-50%) 是定位不能覆盖),usePetDemoShow 两窗共用(usePetDemoActivity 删)。**架构 = 主窗单一决策源广播状态(用户二轮拍板「同步动画状态,不是逐个原始信号跨窗」)**:usePetBehavior 单实例在 MainLayout(常驻层——「隐藏桌宠」只卸渲染决策照跑),行为经 `lw:pet-behavior` 广播(变化即播 + 20s 心跳幂等追平),PetRoamer 变纯执行(props 进、interact 事件回)、FloatWindow 只收广播映射——两窗永不分裂,加行为悬浮窗零改动;首版「悬浮窗自跑一份 + lw:pet-typing 补打字信号」被真机实锤分裂后废弃。③ 点头像误展开 = mac 既有漏气修掉(orb 纯拖动手柄,合成 click 冒泡到 .cap;@click.stop,预览验过点胶囊照常展开)。真机 watch(手感/烦不烦/三形象不穿帮/悬浮窗打盹与辉光环不打架)+ B2 出图拍板见 PLAN「2026-08-31·三批」节。规则变更 = 无,过程级观感件)。前次 2026-08-31·二批(**PC 管家三件:打印 print_file + 系统快照 system_status/启动项 startup_list·toggle + 自动备份**,新 §7.10 节 + §6.8「app 级键三处各一行」实锤补:源起 = 用户问「PC 还有什么需要 larkwing 解决?电脑管家?」→ 过筛定性「不做品类、吸收痛点」(否决记档:杀毒/弹窗拦截/驱动/注册表清理/上网管控;二线:软件装卸 winget/网络诊断),用户拍板「123」三件一批 + 备份四数(每周/留 10 份/默认关/**机器清理绝不经 LLM**——轮转必须 100% 发生不能赌模型记得删、删文件代码面越窄越好)。① 打印 = 一期只 PDF+图片按内容认(Office 指路转 PDF)、Windows 自绘 GDI(pdfium 300dpi 栅格化 + StretchDIBits 等比横放合白底,**不走 print 动词**)、mac lp、列打印机走注册表、pdfium 取用口收口 `pdf::with_pdfium`(第二个 OnceLock = 二次 bind 必炸);② 系统快照 = sysinfo 0.33(MSRV 钉、只开 system+disk)只读聚合(TOP 按名聚合÷核数对齐任务管理器、卷级剩余补上 fs_usage 不管的半边),启动项 = **StartupApproved 标志位与任务管理器同一机制**(可逆互认)、toggle 只改当前用户两类、计划任务一期不碰、「点名才动」进技能(「电脑清理」升级「电脑清理与提速」slug 不变刷内容);③ 自动备份 = `autobackup.rs` 水位线(diary 同款,**不走 jobs 不经模型** §5 物种③)、`backup.auto.dir` 非空即开且选完立即出第一份、`prune_backups` 只认自家命名按文件名时间戳**先备后删**留 10、改名 = 钉住、直接删不进回收站、失败超 14 天且本次也败才 toast 一次(3 天频控)。公共:四件套 ×4(白名单已 grep 核,torrent 教训)+ delegate 五件套判定(startup_toggle/print_file 归 ④ 现场交互排除)+ eval 三守卫(`print-recent-file` **夹具内容故意不是图片,eval 机连着真打印机也绝不真出纸** / `slow-pc-diagnose` / `startup-no-mass-disable`)。Mac 全绿 750〔新增 11 测〕/ clippy 触点零警 / vue-tsc / i18n 镜像 815=815 / 预览设置卡 zh+en 验过;**Windows GDI/winreg 代码 mac 编不到(cfg(windows)),首编 + 全部真机验收见 PLAN「2026-08-31 PC 管家三件」节**。规则变更 = §7.10 新节 + §6.8 补条,过程级 + 用户拍板〔三件范围/备份四数/打印格式范围〕)。前次 2026-08-31(**唤醒后丢字修复批:交互听音段前滚 1.0s + cpal 通道扩容**,§7.5 新★条 + 「pre-roll 200ms」清单项订正:用户报「唤醒应答完说 123456789,大多数时候识别成 3456789、偶尔 34567」→ 上游 sherpa v1.13.2 源码核对定案 = silero 触发前任何一个 32ms 窗掉阈值就清零重计,断续开头整音节丢在段外、内建回退只有 min_speech+64ms 够不着;偶发尾丢 = 停顿超 hangover 首段定稿后 AwaitTurn 丢帧(机制不动,patience 可调)。修 = `collect_utterance` 自缓冲 + `UTTER_PREROLL_S=1.0s` 段前滚(§4.11 拍板;录制类采集传 0 忠实原声;`CaptureOut` 带 `speech_from`,反幻觉闸/声纹按裸段、ASR 吃整段;`pad_utterance` 纯函数 + 越界回落裸段)+ cpal `sync_channel` 64→512(确认层 ASR 阻塞期溢出丢帧的次要头丢机制)。顺带发现并订正文档漂移:§7.5 锁死参数清单里「pre-roll 200ms(robot 终值)」在 collect_utterance 从未兑现、mod.rs 注释「唤醒/听写链早有同款」不实。Mac 全绿 lib 739(新增 pad_utterance 2 测)/ clippy 触点零警 / 工作区 check;真机 watch 见 PLAN 2026-08-31 节(成败判据 = 唤醒后正常节奏报数字头尾不丢)。规则变更 = §7.5 新★条 + 订正,过程级 + 用户拍板〔1.0s/尾部不动〕)。前次 2026-08-28(**ASR 模型下载修复批:FireRed 源换 gh release 整包 + 「重试」100% 崩溃根治 + 识别模型扩 4 档**,§7.5 源变更/扩档记档 + §8.4 新陷阱:用户报两件——「切识别模型下不了,开代理也不行」+「下载失败点重试 100% 崩」。① 下载死因 = 上游 HF 仓库 `…fire-red-asr2-ctc…` 转 gated,resolve 直链回 **401**、hf-mirror 对其 308 跳回 huggingface 同死(HTTP 拒绝非连通性 → `fetch_candidates` 直连/代理两趟同样 401,**代理救不了**);models.rs 注释里写的「备用源 = gh release 同名 .tar.bz2(已验)」**从来没接进 urls** —— 实测仍活(包内 model.int8.onnx 775MB 亲验)→ `ASR_FIRERED_CTC` 由 `ModelSpec` 改 `TarModelSpec` 走 gh release(吃 gh 镜像展开,国内更稳);两档规格自此不同型 → `AsrModel::spec()` 退役换 `id()`,就绪/下载收口 `VoiceModels::is_asr_ready`/`ensure_asr` 按档派发(调用点 4 处 + retry 臂换 ensure_tar);SenseVoice 的 HF 源仍健康(302)刻意不动(其 gh tar ~1GB 含 fp32 = 默认档下载量 4.5×,不划算),顺带记档 hf-mirror 已变纯 308 跳转不再自代理。② 崩溃根因 = `retry_voice_model`/`retry_download` 是**同步** tauri 命令,在 IPC/UI 线程内联执行(tauri 2.11 源码核实:只有 async 命令才被 `async_runtime::spawn` 派发)、无 tokio 上下文,而 core 的 `retry_model`/`retry_component` 开头就是裸 `tokio::spawn` → panic 穿 FFI abort = 必崩;修 = 两命令改 **async fn**(§8.4 新陷阱条 + core 两方法 doc 补「须在 tokio 上下文内调用」)。③ **识别模型 2 档 → 4 档(同日二批,用户拍板「多备候选防单源挂;下拉 = 名字 + 一句话特点,简短点」)**:+ Fun-ASR-Nano(远场高噪/方言,SenseVoice 兼容导出零新架构,1.13.2 的 `is_funasr_nano` 分支源码核实)+ Paraformer-zh(老牌不同架构 = 真备胎),均 gh release 整包、**本机新 fixture `examples/asr_probe.rs` 真加载真转写双双验过**(Nano ≈9× / Paraformer ≈47× 实时,中文正确);Dolphin/LLM 全量形 Nano/默认档换源均评估过不做(详 PLAN)。Mac 全绿 lib 737 / workspace check / clippy 触点零警 / vue-tsc / i18n 镜像 800=800 / 预览中英四档下拉端到端验过(SkinSelect 弹层宽度上限实锤 → 文案按实测收字数);真机 watch 见 PLAN「2026-08-28」节(成败判据 = 切 FireRed 真下得动〔镜像/代理链〕+ 故意断网点「重试」不崩且重试真重下 + 新两档下载/转写/远场体感)。规则变更 = §7.5 记档(源变更 + 2→4 档 + 档名露产品名半句更新)+ §8.4 新增,过程级 + 用户拍板〔扩档/下拉文案〕)。前次 2026-08-21(**进度条 hover 读数 + 本地缩略图预览**,§7.1 新条:用户报「拖电影进度条不知道当前几分几秒,只能盲拖,能不能像视频网站那样 hover 出时间/缩略图」→ 查代码**先纠前提**(拖动中早就显示目标时间 `displayPos`,缺的是读数离光标远 + **按下之前没有读数**、而 range 点哪跳哪),用户圈定范围 = 时间气泡 + 本地缩略图 + 音频条一起加。① 气泡 = 共用件 `useScrubHover.ts`(零后端零 i18n、全播放路由通用;减半个拇指宽、时长未知恒不出、**夹在面板里不是夹在进度条里**〔带图气泡比窗口态进度条还宽〕、刻意不换掉 range);顺手把音频条也改成「松手才 seek」。② 缩略图只走本地:`thumb_url: Option` = 唯一信号 + relay `Entry::Thumb` 与 `/thumb/{token}?t=` **与四条播放臂分开注册** + 新 `Components::ready`(同步绝不下载,**不为预览图拉组件**)+ `-map 0:V:0?` **大写 V 防挑中封面图轨** + 四道闸单源 relay.rs(量化 10s / 全局 FIFO 48 张〔**不按 token 分**:streams 表从不清理〕/ 串行 1 / 超时 8s)+ 抽不出一律 404 让前端降级(失败格不重试、**攒够 3 个不同格才整片放弃** —— 最右端那格常越过片尾本就没帧)。③ **明确不做**:网络流缩略图(Dash/Remux 没帧可抽,正路 = B 站 videoshot 雪碧图 = 按源分化另一档活)· 起播预生成雪碧图(上百次 seek 集中吃 CPU、换片重来)· 音频缩略图。④ 预览抓到三个通用坑并就地立规:range 的 **UA 默认 margin:2px**(容器宽≠轨道宽 → hover 秒数与拇指差几像素)· **class 撞车**(`.pbtn.track` 已存在 → 新容器改 `.scrub-track`)· 量浮层尺寸用 **nextTick 不用 rAF**(rAF 在隐藏窗口不触发,§8.1)。新增 5 测(4 常跑 + 1 真 ffmpeg #[ignore],真图/量化/片尾 404 都验过)、`cargo test -p larkwing-core` 全绿 / clippy 与 HEAD 对账**零新增警告**(临时 worktree 跑基线,不动工作树——树里有另一会话 WIP)/ vue-tsc / **i18n 零新键** / 预览 ?demo=series + ?demo=player 两处验过映射精度(最左 0:00、正中、最右 = 总长、气泡逐像素跟手)。真机 watch 见 PLAN「2026-08-21 进度条 hover」节(成败判据 = 光标放上去就看到那一刻画面 + 时间,看准了再点)。规则变更 = §7.1 新条,过程级 + 用户拍板〔范围〕)。前次同日 2026-08-21(**克隆录入质量批:参考音 GTCRN 去噪 + 归一不硬削 + 录入体检 + 修 speaker 悬空**,§7.5 新★条 + §4.11 常量清单:用户报「克隆音色播放莎莎声很严重,不知道是录的声音质量不行还是啥」→ **先量再猜**定案(参考音底噪 −49dB ⇒ 输出 −48~−50dB 一比一跟随,edge 云对照是纯静音)= 零样本克隆把录音条件当音色学走;用户当场重录成了第二个自然样本,把因果钉死。两个病灶:环境底噪 + **`peak_normalize` 硬削波(我们自己的 bug)** —— 按 99.5 分位推到 −3dBFS 而语音真峰在其 1.8 倍上,顶部 0.5% 被 clamp 切平(真峰恰 1.0000/30 个平顶/p99.5 正落 0.708 三重铁证);**此条推翻了我先前给用户的「调低输入增益」建议**(录得越轻增益越大、削得越狠)。**去噪选型由用户一句质疑定案**:我先用 ffmpeg afftdn 做探针,用户质疑「这算针对这个环境去噪?换个地方还得重调参数?」—— 判据成立,谱减法要按环境调参且在**音节尾音**留「音乐噪声」伪影(用户耳测复现)→ 否决;换 **GTCRN 神经去噪(推理期零旋钮)**,真跑 −49→−60dB / 158ms,耳测「外放听不出」。落地四件:① 归一增益按真峰封顶(响度让位于不失真,三测钉着);② `voice/denoise.rs` = GTCRN(0.5MB 用时下载 + sha256)+ 体检纯函数,现场录音与导入两条路都洗、模型不可用则如实告知不挡录入;③ 录入体检(削波归一前量/底噪去噪后量/一次只报一条建议、是建议不是闸),阈值 §4.11 用户拍板单源 denoise.rs = 底噪 −55dB · 削波万分之一;④ 修 `delete_clone` 不回落 `voice.speaker` 的悬空 bug(真机实锤,逐用户清、家人选择不动)。**明确不做**:输出侧去噪 · afftdn 类需按环境调参的滤波 · 16k 采集链改 48k(高频天花板,等干净录音立住再评) · num_steps 4→8。Mac 全绿 lib 703 + 集成 20〔本批新增 8 测〕/ clippy 触点零新警 / vue-tsc / i18n 镜像 795=795 / 工作区 check;探针 fixture 留 `examples/denoise_probe.rs`。真机 watch 见 PLAN「2026-08-21 克隆录入质量批」节(成败判据 = 安静环境重录后莎莎消失且尾音无刺耳点)。规则变更 = §7.5 新★条 + §4.11 清单,过程级 + 用户拍板〔两个阈值/落地范围〕)。前次 2026-08-19(**旅游计划(国内)支持 = 出厂技能「规划旅行」+ weather 7d 档 + eval 守卫两条**,§7.9 技能 13→14 记档:用户问「旅游计划怎么支持,不着急写代码说方案」→ 按 §5 定性 = **组合任务不是新能力域**(「没预设过的组合任务」试金石的教科书场景——调研 web 三件套/web_render、编排归模型、产出 fs_write_text+briefing、跟进 reminder/盯网页变化/send_file 链条本就齐,缺的是方法论不是能力),**零新任务工具/零新 UI/零行程域**;方案期明确否决 travel_plan 工具、行程日历页(反 §5/§3)、高德地图 API(要用户 key 违开箱即用,模型常识 + web_render 开地图网页版兜底,真机撞了再议——别当 TODO 捡)。用户拍板三点(纯技能落点/weather 7d 顺带做/技能九条口径)+ 点名「考虑子 agent 加速资料收集」→ 技能第 3 条写实 delegate 分头(景点/交通/住宿各派一路、一轮多派真并行 + plan_set 先列步骤;**这是 08-18 plan_set/delegate 批的第一个天然真机考题**,真机验旅行 = 顺带验那批机制)。技能九条要点:问齐基本盘 / **关键事实现查绝不凭记忆**(调休、预约规则年年变,注明查证日期、查不到写「以官方为准」)/ 分头办事 / 编排分寸(老人小孩 2-3 点/就近/雨天备选)/ **行程落文件 + briefing 记路径 = 跨会话与手机接续的机制不是可选项** / 提醒+盯票+weather 7d/watch_set 接线 / 天气分寸(7 天内才有预报,远期只说季节常识)/ 支付恒交用户(登录验证码小窗人机接力)/ 偏好 remember 归人;内容纪律 §6.5 全文零真实地名。weather 机器面:`When::SevenDay`(parse 收 7d/7天/七天),和风 `forecast_leaf`→`weather/7d`(**免费订阅本就含**)、Open-Meteo `forecast_days_param`→"7",两源各一个可测小纯函数、Now/Today/3d 行为零变化,工具 schema enum 加 "7d"。eval 守卫 `trip-plan-asks-first`(信息不全先追问,禁 fs_write_text/delegate 抢跑)/ `trip-plan-research-current`(年年变的调休必 web_search 现查)。Mac 全绿 lib 691〔新增 2 测〕+ 集成 20 / clippy 触点零新警(两处报警均存量:scenarios is_none_or MSRV、parse_ipip 字符比较,不在触点行)/ 工作区 check;i18n 零新键(纯数据 + model-facing)。真机 watch 见 PLAN「2026-08-19 旅游计划」节(成败判据 = 一句「假期带家人去某地玩几天」端到端:不用人踢、行程落文件、提醒设对、查票撞登录正确交小窗、支付停在确认卡)。规则变更 = §7.9 +1 记档,过程级 + 用户拍板〔落点/7d/口径/子 agent〕)。前次 2026-08-18·三批(**分头办事 = 子回合委派 `delegate` + HUD 停止钮通用件**,§6.5 新★条:源起 = 用户看真机「探查任务好多步、处理又好多步、再加上错误」问「有没有必要增加子 agent 的能力」→ 先按「是否合适」评估拆三病(断链归 plan_set 待真机 / 同质批量归 bgtasks / **探查中间结果体积没人治** —— 几十轮原始结果灌主回合、cap_messages_tail 盲裁、安全阀触发即整链缓存 miss),用户拍板「还没发版,要做就做完整」+ 三轮收敛:简报上限 2000→**10000 backstop**(「调研要带背景」,长材料落文件给路径)、同步/异步之问 → **两段式**(30s = IN_TURN_WAIT 单源,等不完不作废转 bgtasks 续跑 + report job 唤回合;如实记档 = 大多数探查走转后台路,与计划槽〔此刻〕咬合)、**HUD 停止钮**(「做错了停不掉烧的都是 token」→ 做成通用件:TaskView.bg + bind_bg + bg_cancel 命令直连 task_cancel 同一协作旗标,按钮不绕 LLM §7.1,七类后台任务全 bind + 顺手修 web 大文件 job「话术说在任务条上、实际没 HUD 卡」漂移)。机制 = trait SubAgent 接缝(tools 定义 engine 实现,webrender 镜像方向;EngineSubAgent 锚父回合 user_msg_id → usage 如实含子成本)+ 子回合复用唯一回合循环(assemble_request 同源新鲜上下文 + delegate_injection 物化 + Turn ephemeral 模式 persist_row_if 零落库/不点灯/汇报 = 最后一段收尾文本)+ 工具面 = 场景白名单 − SUB_EXCLUDED(四类排除单源;**golden 枚举保留集 34 = 新工具五件套**)+ 深度 1 双锁 + grants 共享父回合 + join_all 白拿真并发 + 并发满 4 如实退回。明确不做:嵌套/便宜档路由/事件桥进想了想/子回合持久化/排队。**落地当日按用户要求起双 agent 评审(并发/生命周期 + 语义/接线两镜头,零 Critical、核心机制双双判扎实),逐条核实后加固 9 处**:delegate_injection 免责段(LAWS 点名的工具在子回合被排除,不说清白烧一轮——承诺与工具面脱节坑型)/ web·ftp 下载卡停止钮原报「下载失败」改「按要求停下了」/ 取消集成测试补真「无孤儿」断言(FakeLlm.remaining(),ephemeral 不落库原断言看不见孤儿)/ fs_undo 移出子回合(全局 latest 会撤到父回合批次,golden 34→33)/ 空汇报绿卡改 fail / plan 嗅探与 apply_injection 补纵深门 / **预存在**的 bgtasks finish 双汇报竞态补早退 / 删重复 thinking 读;记档 = web_render 窗上限 2 与扇出 4 互挤(watch)、bg 取消检查在连续流期间最迟本轮流尾生效。修后 lib 689 / 集成 20 全绿、delegate 两测 5 连跑无 flake。Mac 全绿 lib 689 + 集成 20〔新增 5 测:delegate 参数/无通道/白名单 golden 3 + 集成 2(汇报回填·会话零子行·usage 锚点·任务卡 running→done、取消级联零孤儿)〕/ clippy 触点零新警 / vue-tsc / i18n 镜像 791=791 / 工作区 check 含 src-tauri;真机 watch 见 PLAN「2026-08-18·三批」节(成败判据 = 大面探查真会派 + 想了想不再被原始结果灌满;DeepSeek 简报质量与 plan_set 同一张考卷)。规则变更 = §6.5 新★条 + §7.2「仅这次」语义延伸记档,过程级 + 用户拍板〔做完整/30s/停止钮/简报上限/双 agent 评审〕)。前次 2026-08-18·二批(**「计划」= agent 会话内工作备忘:plan_set + 计划槽 + 罗盘自检 + HUD 卡**,§6.5 新★条:源起 = 用户真机实锤「NAS 整目录授权给 agent 整理/补歌词,干到一半要人踢一脚『继续剩下的』」——先按「是否合适」评估(用户要求不着急、先聊),拆出三成因:① 自检句纯刹车(只说「做完了就停」没说「没做完别停」,批量干一半顺台阶下车);② 跨回合断链(补词批量转后台 job,收尾唤回合只带「配好 N/没找到 M」,「整个任务还剩哪些」无机制载体,汇报完就地收工);③ 超长回合 `cap_messages_tail` 把任务原话与模型自己的分解裁出上下文。方案三选一(A 只调话术 / B 计划槽+工具 / C 持久任务域),用户逐轮拍板收敛到 B 的轻形态:「会话内 agent 自己接住就行,任务跨天那是任务的事」+ 常量「稍微大些」+「HUD 展示下,任务 title 进度啥的」。机制四件全走现成先例:plan_set(BASE_TOOLS,end_conversation 同族,全量替换、无状态、解析单源)+ 回合循环嗅探写 `SessionSlot.plan` + 发 `AppEvent::Plan` 快照(Confirm 卡先例;删会话空快照收卡;刻意不走 tasks 总线的 drop=fail 语义)+ 「此刻」缝一行(跨回合断链的解)+ 自检句罗盘化(原话截引 400 字 + 未完项 + 「别停下来问」,不调工具也独立生效);LAWS 新「干长活」法条点名(行为纪律必须常驻推送,工具描述拉取式治不了「压根没想到」);few-shot 刻意不加(undertrigger 再补)。§4.11 常量单源 `tools/plan.rs`:30 条/120 字/标题 30/截引 400。明确不做:持久化/跨会话/管理页/优先级/依赖(§9)、名字不叫 todo(撞用户面待办)、回执小票。Mac 全绿 lib 686 + 集成 18〔新增 11 测:plan 解析/格式 7 + 自检罗盘 3 + 端到端集成 1(槽写入→快照→次回合〔此刻〕背诵→删会话收卡),LAWS/BASE golden 各扩〕/ clippy 触点零新警 / vue-tsc / i18n 镜像 786=786 / 工作区 check / 预览 ?demo=plan 主窗 HUD 卡 + ?window=float 悬浮窗行双验、零 console 错;真机 watch 见 PLAN 2026-08-18「计划」节(成败判据 = NAS 量级批量任务中途不再要人踢;DeepSeek 遵循度/陈计划治理长观)。规则变更 = §6.5 新★条,过程级 + 用户全程拍板〔动机/方向/常量/HUD〕)。前次 2026-08-18(**fs_find 返回文件夹 + 广度优先浅层先出**,§7.2 新条:用户真机实锤「米老鼠四季被认成两季」——fs_find 只返回文件(季文件夹骨架不在结果里)+ 第一页 50 条被第一季灌满;用户提「目录也返回 + 改广度遍历」,盘点采纳并点破关键环节 = **显示顺序由收齐后的纯路径排序决定,光换遍历 sort 会把遍历序抹掉,真正要换的是排序键**。修 = 文件夹同口径算命中(以 / 结尾、照样往下钻)+ 收集广度优先(撞 1000 上限浅层扫全 + 每层按名排序 → 截断集合确定,顺手修掉旧 DFS 撞上限收进哪些靠 read_dir 原始序的隐性不确定)+ 排序键改 (深度, 路径);深度可达层数不变、few-shot 落空话术同步。**「截断时报命中分布」评估过不做**(用户拍板:针对单 case、破坏原语单纯性,case 解不完)。Mac 676+19 全绿〔新增 2 测:目录命中/深度序判别、上限浅层不挤掉〕/ clippy 触点零警。规则变更 = §7.2 新条,过程级;真机 watch 见 PLAN 2026-08-18 节)。前次 2026-08-17(**0.2.27「本地播放管线重做」补记 + 「多音轨(≥2)恒进管线」维持拍板**,§7.1 新两条 + §8.1 六批句订正:用户问「多音轨恒进管线是不是可以作废——后面做过音质优先、响度也分开了」→ 按代码对账两件都属实(能 copy 就 copy / 响度均衡移前端 useAudioGraph 播放出口),但 **v0.2.27(2026-08-07)整批漏回写本文件**——PLAN 有完整 ★ 节、CHANGELOG 有节,§7.1/§8.1 空白,「文档会漂」再实锤(补记以代码为准:mac_native 白名单实为「降级兜底」、非 PLAN 设计稿的「删除」)。拍板 = **维持恒进管线**:三亏瘦身一亏——响度腿断、切轨腿本不构成理由(切轨永远重建),只剩「直传选不了轨」平台行为(mac 全轨混播实锤 / Win 无选轨 API、默认轨行为未验);0.2.27 后管线对兼容轨零损 copy,作废只赚秒开;**收窄放宽(仅 Win + 默认轨直传)评估过不做**(平台分叉 + 轨号标签可能撒谎 §3.5),别再当 TODO 捡。规则变更 = §7.1 补记两条 + §8.1 订正,过程级 + 用户拍板)。前次 2026-08-15(**真机四问一批修:fs 读类分页 + 汇报与提醒分家 + 想了想药丸位置**,§7.2 新「读类三原语一律一页+报总数+续读起点」条 + §7.1 ③ 四修条改写 + §6.5 药丸位置句:用户一次真机会话报四件,查完是三个根 —— ① fs_list/fs_find 截断后**没有任何续读口子**(fs_read_text 早有 offset,读类那两个漏了)→ 都加 offset,并按用户追加的要求**把总数一并给出**(模型据此判该翻页还是该换更具体的条件);fs_find 为给总数不能再「凑够一页就停」→ 扫到 `FIND_SCAN_MAX=1000`(**新常量,§4.11 待用户拍板**)+ 收齐后排序(顺序稳定分页才对得上)。用户同轮提的「超长输出一律落临时文件让 agent 去翻」通用包装层**评估后不做**(理由已记进 ffmpeg_run 条:三种病因只救得了一种,而那一种加 offset 更省且没有快照过期风险)。② 后台汇报借 jobs 唤回合、`wake_turn` 一律广播 `kind:"reminder"` → 打字支使的活跑完却被 TTS 念出来 + 系统线贴「⏰ 到点了」+ pending 期间冒充提醒挂在提醒页 → 新 kind `report` 贯穿(event payload 换标签「✓ 忙完了」/ 广播分流 → 桌面不念〔前端零改〕/ 三个 list_* 视图滤掉、触发走 `due()` 不动),**并就地立接线纪律:新增 activity kind 必须同步 channels::outbound_loop 放行表**(否则静默丢掉「手机上支使的活推回手机」)。③ 「想了想」药丸摆正文下方与 conversation_trace 封口语义相反(药丸里全是这段话之前发生的事)→ 移到该段正文之前 + 三条间距规则;**同日追加**:展开层里「思考一坨在最下、上面一排工具」同样反时序,且工具行只有拟人化描述看不到有效信息 → `TurnTrace` 两个桶(`steps`+`reasoning`)改成**一条时间序队列** `items`(Thinking/Tool 交错,live 与回放同构),每条默认一行人话、**再点一下**才露细节;在飞工具条目细节未落库 → 不给箭头也点不开(不装有内容);「N 步」只数工具(`traceTools` 单源)。Mac 670+17 全绿〔新增 5 测:list/find 分页、report job 视图隔离、wake_turn report 贯穿、想/做交错顺序〕/ clippy 触点零警 / vue-tsc / i18n 镜像 782=782 / 工作区 check / 预览 ?demo 验过两副系统线(zh「忙完了」· en「All done」)、药丸位置、展开层交错与两级点开 + 零 console 错;真机 watch 见 PLAN 2026-08-15 节。规则变更 = 四处过程级约定 + 一个待拍板常量)。前次 2026-08-14(**唤醒自听回声修:应答词精准剥离**,§8.2 ★命中即出声应答条②改写:用户真机报「唤醒后它答『哎/我在』但没在听、像立刻退了」→ 代码对账破案 = 出厂应答词表 3/4 是多音节,应答音(cpal 直出)被麦收进转写后「逼踢我在」超尾随豁免 → 每次孤立呼名被推去旁听仲裁 → 模型判无实质内容**整轮蒸发**(不落库不留痕,设计如此)+ 回待唤醒 = 用户看到的「像没唤醒」;偶尔正常 = 抽中单音节「嗯?」。顺带订正旧假设「browser 源 AEC 消自播应答」——分平台不可赌(mac VPIO 消得掉、Win AEC3 只消 WebView 自播,cpal 直出是另一路 OS 流)。修 = `PromptPlay` 带回本次播的文本 + `triage_transcript(played_ack)` 拼音层精准剥离(**只剥本次这条、只剥紧跟呼名的前缀**,用户确认过「精准 vs 词表」的取舍:词表扫会误伤真实续句同词开头;同音异字拼音层天然容忍;半截泄漏不剥落仲裁兜;回落「叮」= None 零剥离)+ 旁听上抛文本 `strip_ack_once` 字符层 best-effort 抠自家应答。Mac 666+16 全绿〔新增 2 测:剥离八形 + 抠除三形〕/ wake/prompts 触点零警;**真机复验 = 成败判据**:喊「逼踢」→ 应答 → 直接说指令能被听进(词表四条轮着撞),日志「唤醒确认」行不再满屏「呼名+续句(我在)」(PLAN 2026-08-14 节)。规则变更 = §8.2 ②记档改写,过程级)。前次 2026-08-13·四批(**桌宠戏份 A 层**,纯前端观感、零后端零 i18n:体验层盘点用户圈选 #10 并拍板「悬浮窗头像也一起」。后台任务/思考/放歌 → 线稿 SVG 道具 + CSS 动效叠加在现有形象帧上——搬箱子(下载/解压/打包…)、放大镜(扫盘/看网页/解析/找歌词)、头顶「…」泡(思考)、音符+停驻摇摆(放歌);**零新美术**(A 层拍板,B 层 = 逐帧动作帧等真机看腻再按 make-roamer-frames.py 规范出图,`CharPack` 加戏份帧槽即吃)。解析器单源 `usePetActivity`(任务>思考>放歌;INSPECT_KINDS 四种拿放大镜、其余任务缺省扛箱子=新 kind 免登记),道具组件 `PetProp` 只用语义 token 换肤跟随、与角色无关三套形象一次全覆盖;主窗 PetRoamer 全套(手上道具随朝向翻面、头顶浮标不镜像、摇摆只在停驻防打架步态),**悬浮窗 orb 右下 17px 迷你角标**(信号全现成:useAgentMood 镜像/useFloat.running/mediaPlaying;think 不出角标——orb 辉光环已表达,主窗藏托盘时角标 = 唯一「在干活」线索);?demo 3.2s 轮换戏份纯看观感(两窗共用 usePetDemoActivity)。vue-tsc 零错 / 预览两窗验过(结构采样:五态全轮到、token stroke 生效、道具矩形落位、float 角标三态+think 如设计不出)/ 零 console 错;真机 watch = 戏份切换即时性、摇摆/道具观感、17px 角标可读性(PLAN 四批节)。规则变更 = 无,过程级观感件)。前次 2026-08-13·三批(**属性原语 fs_stat + fs_list/fs_find 捎改动日期**,§7.2 新条:照片按拍摄时间归档的「时间半边」——对账实锤 fs 家族此前连修改时间都不报(fs_list 只有 名字/大小,EXIF 更无读取口)。用户先问「fs_read_text 加参数读属性行不行」→ 评估出结构性矛盾(内容天然单文件 offset 续读、属性天然整批,塞一起 = 双形 schema + read_text 名字读 EXIF 路由不顺)→ 拍板独立 `fs_stat`:批量 ≤300、大小/修改/创建时间 + 照片 EXIF 拍摄时间(归一化)/相机/分辨率 + BMFF 时长(probe_local 白拿、mkv/mp3 刻意不报免子进程);fs_list/find 顺手捎改动日期(测试 starts_with 断言天然兼容);三工具描述互相指路;整理文件技能补接线;依赖 +kamadak-exif(纯 Rust 零依赖),EXIF 单测 = Writer 手拼 APP1 JPEG 回读。Mac 全绿 664+16〔新增 4 测〕/ clippy 触点零新警〔media/mod.rs 6 条均为离触点甚远的存量〕/ vue-tsc / i18n 镜像相等 / 工作区 check;真机 watch = 真 iPhone HEIC·微信导出照片 EXIF 存活率、Win created time(PLAN 三批节)。规则变更 = §7.2 新条,过程级)。前次 2026-08-13·二批(**能力四件:磁盘占用 fs_usage + 出码 qr_encode + 亮图 show_image + 出厂技能 10→12**,§7.2/§7.8/§7.9 各新条:能力对账荐 6 项 → 用户圈选 ④⑤⑥,再当场追加 ⑦「show picture,用了就直接出现在对话框」并拍板通用定位(不绑二维码,视觉脑看照片也顺手亮)。① `fs_usage` 只读原语(TOP 15 子目录 / TOP 10 大文件 §4.11;引擎 `usage.rs` 纯同步、不跟 symlink、没权限**计数如实报**;`media/usage.rs` 30s 转 bgtasks archive 同款;**顺手修 bgtasks「total=0 不显分母」**单源 `progress_frag`——总数未知的清点型任务从此不显 0/0,存量任务显示零变化);② `qr_encode`(qrcode 依赖白拿;`qrcode.png`+dedupe;上限按 M 档实容收紧 2000 字节——方案期「约 2900」是 L 档数,如实更正;回环测试 = 出码后自家 decode 认回);③ ★`show_image` = read_image 镜像姊妹:字节进用户发图同一内容寻址仓(`save_image_blob`/`image_ext` 上移 files.rs 单源)、`ToolOutput.shown` UI 专道**零字节喂模型**、新 `TurnEvent::Shown` live 插在飞气泡组 + tool 行 payload.attachments 落库派生(前端 `toUiList` 挂邻近 wang 段;**持有点恒 = wang 消息 attachments,单一持有点不双渲染**)、渠道回合退回指路 send_file;④ 技能 +2 = 盯网页变化(天级三档如实 / 内容自包含带基准 / 变了才开口 / 续盯撤旧设新换基准)+ 电脑清理(fs_usage 下钻 / 点头才动 / 恒回收站 / 系统目录绝不碰)。Mac 全绿 660+16(新增 15 测)/ clippy 触点零新警(turn.rs「tests 后置」为 HEAD 即有的存量结构警,不动结构记档)/ vue-tsc / i18n 镜像两侧相等 / 工作区 check / 预览 ?demo 验过图卡渲染与两步轨迹;真机 watch 见 PLAN「2026-08-13 二批」节。规则变更 = 三节新条,过程级)。前次 2026-08-13(**web 浏览三增强:render 通读 read + save_pdf + fetch 表格保真**,§7.8/§7.4 各补条:用户问「web 浏览有可增强的地方吗」→ 对账荐 4 条,圈定 1/2/3(**CDP 可信输入继续等真机撞上率实锤,别提前付**)。① `read` = render 会话通读:当下 DOM 正文抽全定格进会话(120K 字符封顶),offset 从定格切片(6000/次)——登录墙后/JS 长文读全,绕开「快照截断刻意不动」的 offset 不稳顾虑(定格不随步重拍);② `save_pdf` = 当前页存 PDF(mac createPDF / Win PrintToPdf〔mac 编不了,真机 watch〕,文件名取页面标题 + dedupe,dir 同下载闸);③ web_fetch 表格转 markdown(首行表头、行30/列8 封顶如实注明、布局表照旧拍扁、ego-tree NodeId 判 td 归属防重复收——短单元格〔价格数字〕原先直接被 8 字闸丢掉)。两个观察形态与动作互斥「单独一步」;READ_JS 预览浏览器冒烟过。Mac 645+16 全绿 / clippy 触点零警 / vue-tsc / 镜像 774=774;真机 watch 见 PLAN「2026-08-13」节。规则变更 = §7.8/§7.4 补条,过程级)。前次 2026-08-12(**四件一批:压缩包原语 + 出站文字 send_text + 出厂技能扩充 + 播放条滚歌词**,§7.2/§7.7/§7.9/§7.1 各新一条:能力对账后用户圈选三件、追加第四件 = 真机实锤「转发一句话/链接到我的微信」被答只能发文件(出站缺文字姊妹原语)。① `fs_unzip`/`fs_zip`——按内容认格式(zip/rar/7z 魔数)、解压恒落全新文件夹(永不覆盖天然成立、不进 fsops)、zip-slip 逐组件重组、GBK 文件名 GB18030 回退、50GB 闸与 torrent 同口径、带密码盘点期就要、30s 转 bgtasks(`IN_TURN_WAIT` 单源上移);依赖 = zip 补 aes-crypto + sevenz-rust2 0.7(被 rust-version=1.77.2 钉版)+ unrar 官方源码绑定;**rar 无本机夹具 = 真包真机验、unrar C++ 过 Windows MSVC 也是 watch**。② `send_text`——`outbound::send_text` 汇三渠道既有 push(零新协议代码),微信挂起件 `PendingSend` 扩 text 条目(serde default 向后兼容、与文件混挂补发),与捎话(reminder_set for)的分工进工具描述。③ 出厂技能 4→10(定时播报〔内容物化纪律〕/音视频加工〔配方〕/看片守约定〔没约定不发明规矩〕/存歌进曲库/报听写〔TTS 无逐词停顿如实记档,不行就撤〕/睡前故事)+ 既有三条补压缩包接线。④ `NowPlaying.lyrics` 捎旁挂 .lrc(仅本地音频;GBK 回退;>200KB 不带)→ 前端 `useLyrics` 解析 + PlayerBar 上方当前句滚动 +「词」钮(`ui.lyrics`);**无时间轴 .lrc 一期不显示**(一行放不下、假轴更糟——原「静态显示」倾向在一行形态下不成立,如实改判)。Mac 全绿 640+16 / clippy 触点零警 / vue-tsc / i18n 镜像 768=768 / 工作区 check 干净 / 预览 ?demo=player 验过滚词与开关;真机 watch 见 PLAN「2026-08-12」节。规则变更 = 四节各加条,过程级)。前次 2026-08-07(**番剧多集发现**,§7.1 新条 + 发现由两路变三路:用户贴 `bangumi/play/ep742483`(52 集)只放一集就停 → 排查定案**不是解析漏字段,是端点家族不对** —— 番剧是 PGC 体系、与 UGC 稿件两个平行命名空间,`extract_bvid` 对 ep 链接恒 None 直接退化单集;**关键实测:番剧集自带的 bvid 拿去问 UGC `view` 端点是 `-404`**(所以「把 BV 抽出来」那条路是死的、队列必须存 `bangumi/play/ep` 形)。修 = `episodes()` 加第三条分支(`extract_pgc` 认 ep/ss 两形 + `pgc/view/web/season` 免 WBI + `parse_season` 纯函数;载荷在 `result` 不在 `data`;只收正片、`section` 花絮不并进队列;`series_key` 用 `bili:pgc:` 前缀**防与 ugc_season 的 `bili:season:` 同号串味**),**队列机器/build_queue/续播记忆/前端/嘴控一行未动**(来源无关的红利)。**搜索半边用户拍板不接**(实测:`search_type=media_bangumi` 回风控 HTML 要 WBI;免签名 `search/all/v2` 对这季 4 种查询变体含官方全名 PGC 命中 0 条,连 Bing 都搜不出 → 不划算;贴链接是主入口,免链接复用靠 briefing 记链接);**追更用户再次拍板不做**(并在 PLAN 订正「结构上追不了番剧」已不成立、但结论不变,别因此把追更当小活捡起来)。Mac 597+16 全绿(新增 4 测)/clippy 触点零警/生产构建干净/**真网 `episodes()` 整链验过**(`#[ignore] real_bangumi_season`);**真机 watch 见 PLAN「番剧多集发现」节**(成败判据 = 放完自动跳第 2 集)。排查副产品:工作树里撞上另一会话的 WIP(probe_local → Option 重构)把**测试构建**弄挂了 24 个错,按记忆纪律没碰它、改在临时 worktree 里验(生产构建两边共存无碍)。规则变更 = §7.1 新条,过程级)。前次 2026-08-04(**技能(工作手册)落地**,§7.9 新节:源起 = 用户「调研业内 skill 现状 + 给 Larkwing 方案」→ 两路调研(官方规范演进:2025-10 发布 → 2025-12 开放标准 agentskills.io → 40+ 产品采纳;采用与实践:**消费级产品〔Manus/Devin/ChatGPT/豆包〕全部收敛到「口头教 + 沉淀带批准」、无一家向消费者暴露文件形态;undertrigger 是官方自认第一坑;Snyk 扫描社区市场 36% 有缺陷、2026-02 真实供应链攻击**)→ 我初版方案「寄生 briefing 零新概念」被用户当场推翻并升级:**技能 = agent 的(恒全局,与用户无关),记忆 = 与用户的约定(归人)**——流程知识需要「看得到/看得到触发/能停用」的可管理性,这是它与记忆的本质区别 → 专门「技能」一等导航 tab(§3 零新概念的**有意突破**,用户拍板)。四轮拍板全落:一等导航 / 统计 = 总次数+近7天+最近(触发唯一定义 = skill_lookup 命中,skill_hits 流水现算)/ 沉淀最保守档(明说才记)/ 就叫「技能」;hub 不接(安全实锤 + 语义对不上),真需求落成**出厂技能库**(assets/skills.json 四条:放歌放视频〔从 persona 抽出=顺提示词,persona 回归纯性格〕/ 网页上办事 / BT下载 / 整理文件;boot sync 按 slug 刷内容保 enabled);三层渐进披露**全在库里不走文件**(L1 索引恒常驻无折叠〔backstop 64 条满了如实退回〕→ L2 skill_lookup → L3 附录节+机器生成导航行);LAWS 两本账 → 三本账 + 新「技能」节(pushy)+「说人话」白名单加「技能」;few-shot 加教技能示范(占位纪律);skill 三件进 BASE_TOOLS。Mac 全绿 593+16〔新增 12 测〕/ clippy 触点零警 / vue-tsc / i18n 镜像 752=752〔新增 nav+skills 组+tool 三键〕/ 预览 ?demo 中英端到端验过(tab/列表/统计三数字/展开正文/启停/停用半透);真机 watch 见 PLAN「2026-08-04 技能」节(真模型教技能全链/触发遵循度/内置升级刷新/渠道语音教技能)。规则变更 = §7.9 新节 + §3 tab 突破记档,用户全程拍板)。前次 2026-08-03(**ffmpeg 直通 `ffmpeg_run`**,§7.1 七原语 + 新条:用户拍板「一个 String 传 ffmpeg 参数,使用最灵活开发最简单」→ 方向采纳、三处收口(灵活性不打折):① args 用**数组**不是单 String(零转义直喂 Command::args,免 shell 分词/引号/中文名地狱);② **输出永不进 args**(独立 output/dir 参数 = 授权圈 create + 永不覆盖 + 临时件的挂点;从裸参数认输出得认全几百个 flag 谁吃值,认错一个 = 授权圈旁路,提出来问题整个消失);③ 边界闸 `scan_args`(输入过 read 闸、滤镜内嵌路径〔subtitles=/ass=/movie=〕抽出同闸、拒网址/pipe/-y/-f concat·lavfi/旁路写文件 flag/孤裸值)= §6.3「参数无后门」的**有闸例外**。执行不做成本预测:先跑 30s(单源 edit.rs)没完自动转 bgtasks(只在真推进时 beat 保看门狗有效;TempGuard 兜半路死);`-c:v h264` 占位**确定性替换** GPU 编码器(relay `video_encode_args` 提单源、共享探测缓存,结果如实报用了哪个);**同日追加探测形** = output 可选、不给就只探不产原样返回 ffmpeg 信息横幅(用户点破「输出直接还给模型」,media_probe 独立工具方案被取代;「去掉结尾 N 秒」闭环 = 先探时长模型自算绝对点)。**顺手修存量 bug:torrent_download 自 0.2.24 落地起没进 companion.json 白名单**(golden 同漏测试照绿 → 模型一直看不见它,真机「下种子」必答没能力;已补,并就地立规「新工具四件套,收尾必 grep 白名单」)。同批背景:用户报「歌词匹配后很多文件名对不上歌」→ 代码对账 = lyrics_fetch **无任何改名路径**(全仓 rename 均临时件收尾),最可能是模型组合 fs_move 批量改名(fsops 有记录、可 fs_undo/操作记录页撤销)或文件下载时代本就错名;治本方案(配词前 ASR 听核 + read_audio 拉取式听原语)已与用户确认设计、**本批未做**(用户「ffmpeg 先做」,别当已落地)。Mac 全绿 580+16〔新增 7 测〕/ clippy 触点零警 / 完整工作区构建 / vue-tsc / i18n 镜像 729=729〔新增 4 键〕/ 本机真 ffmpeg 命令模板冒烟;Windows 真机 watch 见 PLAN「2026-08-03」节。规则变更 = §7.1 七原语 + 新条,过程级)。前次 2026-07-30(**文件授权圈**,§7.2 大改 = 🔒 变更:用户主动提出「只有明确授权了的目录才可以读写;程序自己的数据目录不需要授权」→ **推翻 §7.2「不设路径门禁」否决记录**(「不强制对话确认」半边不回潮——授权是目录级一次性点头,不是每次写都确认)。方案两轮拍板:① 三档 = 只读 / 可存入(读+新建,用户点破「新建危害比修改删除小、有的允许下载但不允许改」)/ 完全访问,**下载+桌面 = 出厂基线「可存入」正好是中间档**(改删弹卡),模型统一无特殊行;② 撞圈确认卡「一直允许 / 仅这次 / 先不要」三钮;③ 授权父目录自动合并被覆盖的低档子孙(档位更高的子条目保留绝不降级)。机制 = `tools/guard.rs` 单源(规范化含 canonicalize 最深存在祖先 + simplify 防 verbatim + 组件前缀匹配)+ Confirmer 三态化(`ConfirmReply`,审计多一档 allowed_always)+ `ToolCtx::grants` 回合缓存(仅这次 + 已拒不连环弹)+ 17+ 工具接入(接线纪律:新收路径工具一律过 `guard::ensure`;fs_undo 先读记录再 ensure;批量转后台的授权前置到 spawn 前)+ 设置·系统「能碰的文件夹」卡(内置行可升降 + 用户条目三档 + 原生选择器)。**渠道/语音回「允许」恒按仅这次**(永久授权只留给有 UI 的地方);**工具描述刻意零改**(拒绝话术自解释,前缀缓存不失效);免闸区含系统临时目录(scratch 区)。顺手修 SkinSelect 弹层宽度(选项比按钮长被截断 → 锚右缘 max-content)。定位改口 = 「不做任何安全承诺」→「安全带不是保险库」(可逆三规仍是第二层保险)。Mac 全绿 573+16(新增 11 测)/ clippy 触点零警 / vue-tsc / i18n 镜像双侧相等 / 预览 ?demo 三钮卡 + 设置卡 zh+en 端到端验过;真机 watch(PLAN):真撞圈弹卡链 / 渠道回话仅这次 / 语音口头确认 / Windows 路径形(盘符/UNC/大小写)/ 老用户需知目录首次补票体感。规则变更 = §7.2 大改 + §4.11 清单,🔒 级已确认)。前次 2026-07-29·四批(**FTP 下载**,§7.8 新条 + 订正二批的「FTP 明确不做」:用户实锤「我用的荐片大部分都是 ftp 资源」→ 我那句「FTP 基本死了」是按公网大势讲的、对这个场景判断错了,当场推翻。**破掉一个会绕人的说法**:「只有迅雷下得动的 ftp 链」不是协议有门禁(FTP 是 RFC 959 开放标准、没有 UA 可供服务器识别客户端),是**那台服务器已经死了** —— 迅雷从自己 P2SP 缓存按文件哈希给字节、根本没碰 FTP;我们没缓存池故死链变不出来,活的照常下(判据:连不上=服务器死了/连上 550=文件名对不上)。落点扩 `web_download` 一个分支不新开工具(§5 正交性),顺带闭环 normalize_link 拆出的 ftp://(上一批还在拒)。机制 = suppaftp 10 仅 tokio 不带 TLS、**净增仅 4 crate**;新 ftp.rs 解内嵌凭证/非标准端口/百分号编码中文名 + 被动模式 + SIZE 分档 + 流式打点,**档位与 http 一致、进度/取消/汇报全用上一批建好的零新机器**;凭证两路(URL 内嵌优先 → 落回 net.http_creds,自家 NAS 靠它)且 FtpTarget 手写 Debug 遮密码。⚠️ 已知边界 = 中文名 GBK(suppaftp 只收 &str,发 GBK 要 from_utf8_unchecked = **UB 明确不进产品**;撞上报 550,话术点名 GBK 别误判成死链)。记账:不走 net::Client 故**代理缺失**、suppaftp 单人维护、REST 断点续传已验证可用但本版未接。Mac 全绿 562+16 / 触点零警 / 完整构建;真机成败判据 = 真荐片 ftp 链能否连上下完。规则变更 = §7.8 新条 + 订正,过程级)。前次 2026-07-29·二批(**`web_download` 松绑:认证 + 大文件转后台 + 专用链拆封**,§7.8 新条:用户问「ftp/网盘能不能一起下」→ **逐协议过筛,清单本身是结论**。做三件:① `normalize_link` 拆 thunder://·flashget://·qqdl://(它们不是协议、只是 base64 包装,拆不开一律原样退回);② 大文件自动转 bgtasks job(同步档 50MB **必须保持不变** —— 下完接 pdf_to_png 那条链靠它;后台档 50GB 与 torrent 同口径、客户端不设总超时;⚠️ 服务器不报 Content-Length 就转不了档,话术已点明);③ 认证按 host 现查、新秘密键 `net.http_creds` 进 keyring —— **刻意不做成工具参数**(密码走参数会进 LLM 上下文 + 落 payload + 每轮回放),因「写得进读不回」故增删一律后端读改写 + 设置·系统「下载认证」卡。**④ 网盘问题由此解掉(方案层)**:WebDAV 下载 = 带认证的 GET → 坚果云/NAS/Nextcloud 白拿,用户自架 OpenList 即覆盖百度/阿里/夸克/115,**我们一行私有 API 都不写**。⑤ 明确不做且有因:网盘私有 API(**限速是服务端按账号打的、换客户端根本不解决**;PanDownload 作者已判刑;军备竞赛)· FTP(浏览器早删干净、近乎绝迹,拆出 ftp:// 如实退回)· ed2k · aria2 重复造。⑥ NAS/SMB 本就是普通路径别重复建。Mac 全绿 550+16 / 触点零警 / vue-tsc / 镜像 758 / 预览 zh+en 双验(**当场抓修一个 bug:误用不存在的 `common.cancel` 原样露键**)。规则变更 = §7.8 新条,过程级)。前次 2026-07-29(**BT/磁力下载 `torrent_download`**,§7.1 第六原语 + 新条:用户问「电影天堂能不能接」→ 评估否决(野 CDN 月抛/站本身月抛/撞 §7.1 版权口径)→ 反向落到**通用 BT 原语**(链接用户给、我们只下,定位同 web_download)。四轮追问后拍板**做种策略 A**(下载期正常传、下完 pause 停);同轮把两处**我先前说错的**当场更正:① 「不去站扒链接」不是可执行的闸(web_render+LLM 今天就能扒,真边界 = 不写死选择器/不内置搜索/不维护片源目录),② 「下完停做种」不等于不分发(**下载期就在传,这部分关不掉** —— BT 是边下边传;tit-for-tat 决定不传就下不动)。期望管理记档:迅雷会员快是 P2SP(服务器缓存池+离线下载),我们没有服务器集群 → **热门种子相当、冷门救不了**,这正是一期只做「下完再播」的理由(边下边播 = 二期,`Entry::Torrent`+起播闸+欠速检测)。**一期能这么小的根本原因 = `play()` 本就按 `is_local_path` 分派 → 下完落文件夹,本地播放链(HLS/音轨/整夹连播/续播)与前端全部零改**。机制 = librqbit 8.1(rust-tls 对齐全局 rustls,关 axum/webui/sqlx;净增 50 crate)+ 引擎懒建 OnceCell(不用 BT 者零成本、**不平白发 DHT 包**)+ persistence:None(重启不偷偷续种)+ 恒后台 job 吃 bgtasks 全套(此刻/status/cancel/收尾汇报/卡死看门狗)+ bg 新增 running_count_of 给 BT 自己的并发闸。§4.11 拍板常量单源 torrent.rs:50GB/并发3/metadata 60s/停滞 5min/缺省只下视频。`.torrent` 比磁力链可靠(DHT 走 UDP、国内正被限速)已进工具描述。Mac 全绿 545+16(新增7测)/ clippy 触点零警 / 完整工作区构建 / vue-tsc / i18n 镜像 747;真机 watch(PLAN §9)= 国内 DHT 能否拿到 metadata / Win 防火墙不放行时的失败话术 / 下完真停上传 / 中文种子名。规则变更 = §7.1 新条 + 六原语,过程级)。前次 2026-07-28(**会话回溯 + 分叉**,§6.5 新条:用户「聊 6 次第 4 次做偏了想回去」→ 拍板「从这里重新说」(原地硬删,与删会话同口径无确认)+「从这里另起新会话」(无损 fork 当后悔药)两动作(右键菜单 + 气泡 hover 快捷小钮双入口,小钮同日用户追加),BT 气泡「重新回答」与软删/分支树明确不做。机制 = repo 姊妹原语 `truncate_from`/`fork_before` 共用 user-行切点校验;engine rollback 先 cancel 后删镜像 delete_conversation、fork 不取消在飞;fork 恒 ui 渠道 + 空标题走定题同路;**副作用不回滚**(提醒/记忆/文件/流水留着 = 「世界已经发生」);前端两时序坑 = 负数本地 id 先 `resolveUserMsgId` 对账(startsWith 闸宁可不做)+ 回溯前 `turnSeq++` 接管在飞防迟到 cancelled 误 pop;截断保前缀缓存白捡。Mac 全绿 538+16 / clippy 触点零警 / vue-tsc / i18n 镜像 686 / 预览 ?demo 两动作端到端验过;Tauri 真链 watch(PLAN 顶部):负 id 立即重说 / 流式中途回溯 / 分叉列表与定题 / 渠道会话回溯后手机续聊。规则变更 = §6.5 新条,过程级)。前次 2026-07-27·深夜四(**合集下载范围参数 from/to**,§7.1 批量条补六批:真机 110 首合集实锤「话术说可分批、参数没给」的空头承诺——模型被迫绕去 web_render 扒分P链接。修 = `media_download` 加 `from`/`to`(1 起含两端,None 回落头/尾,`resolve_range` 越界报「一共 X 首」),大合集分段/「只要第 N 首」都直达;超容量退回带现算分段建议;登记处标题/收尾汇报带段口径。Mac 全绿 536+16 / 触点零警。规则变更 = §7.1 批量条,过程级)。前次 2026-07-27·深夜三(**后台差事登记处 bgtasks**,§7.1 新条 = 用户「异步任务管理器」规划的落地,四轮拍板:并发 20 backstop /〔此刻〕+task_status 两层 / task_cancel 进一期 / **定期心跳砍掉**〔= reminder_set+task_status 组合,零新机器〕。核实结论:提交器/队列/优先级/DAG 不做(§9),裸日志不喂模型(结构化步进史代替);缺口补齐 = 取消 + 卡死看门狗(10min 无步进判卡 abort 照常汇报)+ 半路断 Drop 兜底汇报。机制 = `bgtasks.rs` 登记处挂 MediaRuntime,票据(beat/miss/is_cancelled/finish)与 HUD TaskHandle 并行,收尾统一插 due=now jobs 唤回合(四修机制收编);两批量 job 迁上;`cap_names` 单源上移。Mac 全绿 535+16 / 触点零警 / vue-tsc / 镜像 684;真机验 = 此刻答「到哪了」、「停下」直达、判卡回报。规则变更 = §7.1 新条,过程级)。前次 2026-07-27·深夜二(**批量收尾回报唤回合**,§7.1 ③ 补四修:用户点破「补 100 个文件 9 个没找到,AGENT 并不知道」——后台 job(lyrics 批量 + 合集批量下载)原先只在任务条红一下、模型零感知、哪 9 个没配上无处可查。修 = job 收尾把成败汇总插成 due=now 的一次性 `jobs` 任务(`report_job_done`,media→store 正向依赖零新机器)→ 现有调度器 ≤30s 捡起 wake_turn 自启回合(忙检/重试/event 行/渠道推送全白拿)→ 模型拿到「配好 N/没找到点名〔封顶 12+等 N 个〕/缺歌名点名」向用户转述并接得住重试;汇总单源 `compose_batch_summary`(回合内/收尾共用),下载批量顺带记「没找到歌词 N 首」+ 登录半路失效点名剩余;工具话术改「跑完会自动回来汇报」。已知观感 = event 行沿用「⏰ 到点了」标签(payload kind 换标签待议)。Mac 全绿 519+16 / 触点零警;真机验 = 大批量跑完 ≤30s 内 BT 自己回来说结果、忙时等你说完再说、手机发起推回手机。规则变更 = §7.1 ③ 四修,过程级)。前次 2026-07-27·深夜(**歌词检索简繁双轨修正**,§7.1 二批条补 ⑤:真机首战「电台情歌」两处实锤——① 检索 bug 破案 = 模型传参全对、LRCLIB 直连也通,但**库里这首是繁体「電台情歌」收录**(16 条带时间轴、简体查 0 条,库搜索不做简繁归一)→ 查询梯子扩简繁双向变体(`character_converter` MIT 纯 Rust 新依赖)+ **台/臺歧义特判**(转换器正字「電臺」vs 唱片元数据惯用「電台」,真查臺形 0 条 → 含臺再补台形且靠前)+ 只按歌名兜底(歌名归一闸防同名不同歌)+ 同过容差挑时长最近;真网回归 #[ignore] 测试已过 = 同参数真命中 synced;LRCLIB 国内直连已验通。② 模型代写歌词实锤 = 歌词库没配上后 BT web_search 只拿到纯文本、手写 .lrc 把 [mm:ss] 时间戳**全编了**(假轴乱滚比没有糟)→ lyrics_fetch 描述补「绝不编造时间轴」;Apple 音乐.app 不读 .lrc 旁挂(只认内嵌 ©lyr/流媒体歌词)= 预期行为记档,「下载时顺带内嵌歌词标签」待用户拍板。Mac 全绿 518+16 / 触点零警。规则变更 = §7.1 ⑤ 过程级)。前次 2026-07-27·晚(**命名/标签 + 歌词 .lrc + 存量补词 `lyrics_fetch`**,§7.1 音乐下载条追加二批条:真机首测截图实锤两病根〔文件名/标签 = B 站标题党原文、artist = 搬运 UP 主〕→ ① 干净歌名/歌手 = 模型抽取传参(`media_download` 加 `title`/`artist`,文件名「歌手 - 歌名.ext」,UP 主只进 comment;正则启发式/音频指纹两路已否——B 站 UGC 无结构化歌手字段,信息抽取归模型);② 歌词两级落同名 .lrc(人工 CC 字幕〔ai-* 滤掉〕→ LRCLIB 免鉴权歌词库,时长容差 ±3s「宁可不配绝不配错」,纯音乐不配,失败绝不影响下载;野歌词 API 不接);③ 新原语 `lyrics_fetch` 给存量曲库批量补词(绝不动音频原件、已有 .lrc 跳过;标签缺失点名让模型从文件名判断带参重试;≤20 回合内、更多转 job,封顶 200;`probe_with_ffmpeg` 扩全局 title/artist 标签解析)。Mac 全绿 515+16 / 触点零警 / vue-tsc / i18n 镜像 676;LRCLIB 国内可达性、真曲目命名/CC 转 lrc/存量批量、播放器滚词 = 真机 watch〔PLAN §9〕。规则变更 = §7.1 二批条,过程级)。前次 2026-07-27(**音乐下载 `media_download`**,§7.1 三原语→四原语 + 新条:用户拍板三件——默认存系统「下载」文件夹、格式跟着源走〔有无损存 .flac 否则 .m4a,mp3 有损转有损档明确不做〕、批量做但合集没说清先问〔eval 守卫 download-batch-confirm / download-batch-explicit〕。机制 = 解析同链换下载格式串 `DOWNLOAD_AUDIO_FORMAT="ba/b"`〔质量优先,大会员 Hi-Res FLAC 有就拿〕+ web_download 同款落盘〔.part+dedupe+防盗链头,500MB 闸〕+ ffmpeg `-c copy` 整理容器写标签全程不转码〔缺席 = 原样保存如实说〕;需登录 ≠ 失败但无自动重下;批量 = 分离 job 逐首下 tasks 进度、`BATCH_MAX=100`、登录半路失效剩余搁置。顺手 `files::human_size` 单源化〔tools::fs 留转发〕。已确认 .flac 本就是音频桶一等公民〔is_audio_ext,用户 HiFi 曲库直传/整夹连播现成〕。Mac 全绿 508+16 / clippy 触点零警 / vue-tsc / i18n 镜像 671;真 B 站单曲 / Hi-Res 真出 FLAC / 合集批量 / Windows 防盗链与中文文件名 = 真机 watch〔PLAN §9〕。规则变更 = §7.1 新条,过程级)。前次 2026-07-21·晚(**微信挂起补发**,§7.7 ② 再补:v0.2.20 的「去令牌重发」降级**真机证否**——iLink 对无令牌发送一样回 ret=-2(用户「虽然提示了还是不行」),会话窗口 = 平台反骚扰设计、协议无任何令牌刷新 API(context_token 只随入站消息来,openclaw 全量类型翻遍证实)→ 治法换**挂起补发**:双拒错误带 `StaleContext` 类型标记(anyhow 链 downcast)→ send_file 工具把「窗口关死」的文件挂 settings KV(`remote.weixin.pending_sends`,48h 单源 `PENDING_TTL_HOURS`,同目标同文件去重)、结果 **Ok 如实说**「已挂起、TA 一开口自动补送、不用重发」并点名是谁(§7.1 AwaitingLogin 同哲学,不算失败);收循环拿到新令牌先 `flush_pending_sends`(成功/文件已删出列、失败回炉、TTL 兜底、`PENDING_MU` 防丢更新);提醒推送刻意不挂(时效敏感)。Mac 全绿 498+16 / clippy 触点零警 / 新增 4 测;**真机验 = 窗口过期时发文件 → 回「已挂起」→ TA 说一句话 → 自动到账**。规则变更 = §7.7 ② 过程级补记)。前次 2026-07-21(**本地放歌连播 + 循环/随机播放**,§7.1 过程级补记〔Mac 全绿 493+16 / clippy 触点零警 / vue-tsc / i18n 镜像 657 / 预览验过播放条三态循环与随机高亮;整夹连播手感、嘴控命中 = 真机 watch,PLAN §9〕:起因 = 能力对账新发现的家庭硬伤——音频桶套用视频「数字骨架」闸(那道闸防平铺电影库误触没错),歌名天然各异 →「放儿歌」永远单曲放完就停,media_play 描述却承诺连播。修 = 音频整夹组队(视频规则零动)+ media_play 收音频文件夹(强制只出声;1 首退化 0 首退回)+ series_key 换目录+audio 桶标记(老音频续播记录一次性失联,拍板可接受)+ **media_control 五动作** loop_one/loop_all/loop_off/shuffle_on/shuffle_off(用户拍板「随机播放加上」,循环按推荐 C 案 = 默认停 + 嘴控开;状态归 core、NowPlaying 全量镜像、ended 改问 auto_next、单曲循环 el.loop 原生无缝、循环开着到头回卷、随机履历可回退)+ 播放条 🔁🔂🔀 按钮与「第 N/共 M 首」文案。规则变更 = §7.1 过程级补记。**同日二批:`~` 路径展开 + 工具描述去内容示例**〔Mac 全绿 495+16 / 触点零警;Windows 真机 watch = ~ 展开/中文用户名〕:用户真机抓包「记一下 ~/Downloads 有电影」被写成「电影在 D:\\Movies 和 ~/Downloads」——追因 = briefing_lookup 无记录、D:\\Movies **抄自 briefing_write 描述示例**(§6.5 泄漏口扩面实锤:工具描述示例内容同样被抄;已立规并清 briefing/fs/desktop 三处)+ 全链无人展开 `~`(fs 当相对路径、is_local_path 拒收;模型 reasoning 自认「以后用的时候再展开」)→ 新 §7.1「路径走 `tools::expand_home`」条(§4.4 arg_bool 同族,接入 fs 家族 + media_play/qr/read_image/pdf/send_file/desktop open/web_download/web_render 全部路径入参)。规则变更 = §6.5 泄漏口扩面 + §7.1 expand_home 条。**同日三批(描述修完重编重测仍复现 → 真主源 = few-shot)**:companion.json 的 briefing 合并示范(『电影在 D:\\Movies;音乐在 D:\\Music』)与泄漏逐字同构——示范路径全换自标占位(D:\\示例片库族)+ LAWS 出厂示范补「绝不把示范内容写进真实数据」;排查教训 = few-shot 在 `assets/scenes/` 不在 src/,grep 漏扫连错两次定位。**同日四批(Mac 看片绿屏卡死两修)**:BD mp4(H.264+AC3)在 Mac 壳被切进手写 MSE 自适应 → WKWebView 绿帧 + `play()` 被拒 paused 弹回 true、12s 看门狗(带 !paused)不触发无兜底——① probe 平台感知(mac 白名单 AC3/E-AC3/ALAC/HEVC 直传,注入可测双矩阵,Windows 零变化)② 看门狗补 `readyState<2` 盲区(Windows 同受益);§8.1 新 Mac-only 条,mkv 在 mac 的 MSE 链记档为已知边界。**同日五批(音轨选择,用户拍板 C 案「UI 和 AGENT 都能切」)**:probe 音轨清单(BMFF trak/mdhd 语言 + udta name + ffmpeg stderr 语言/title)→〔此刻〕列清单 → `media_control` 新动作 `audio_track` + 播放条/视频浮层按钮;切换**一律重建管线** `-map 0:a:{n}` + resume_at 原位续播——首版 mac 直传走 audioTracks 就地启停,**真机当场证伪**(WKWebView 播放中启停不重路由 = 切完静音),就地启停降级为仅「起播收敛」(治 QuickTime 全轨混播 = 「中英都有」的根治);逐轨兼容判定(选中轨才决定转不转);Mac 全绿 503+16 / 预览验过两处按钮,真机 watch 见 PLAN §9。§7.1 新「音轨选择」条。**同日六批(「音频都转」拍板)**:真机复验混播仍在(audioTracks 起播收敛也未生效 = 该 API 整个不可用)→ mac **音频**白名单回滚(AC3 两平台同矩阵转 AAC+响度链,白名单只剩 HEVC 视频)+ **多音轨恒进管线**(`-map` 单轨输出,混播物理不可能);mac 管线出口保持自适应 MSE **交真机定案**(「MSE 解不出」经用户追问降级为未定位推断——append 全成功无错误回调、同片直传可解;§8.1 归因订正 + `[adaptive]` 诊断日志埋点),卡则看门狗回落 muxed、再不行切 /m/ 后备。**次日真机定案(2026-07-22)**:B 形态——自适应卡 12s 看门狗回落 muxed(经 shaka 播通,WKWebView MSE 无辜),5s 快照锁定真凶 = 首视频样本 PTS≈0.1 + WebKit 播放头须在缓冲内的严格度 → localAdaptive 加最小 gap-jump 根修;音轨名 = 文件 und 定案(「音轨 N」诚实兜底);诊断先行的打法一轮破案,Safari harness 销案)。前次 2026-07-18(**微信过期令牌降级**,§7.7 ② 补记,随 v0.2.20 发版:真机 send_file 撞 `sendmessage ret=-2 errmsg=prepare failed` = **context_token 过期**(非限流非格式错——npm 拉 openclaw 2.4.6 源码逐字段对照全一致;hermes-agent PR#62386「过期令牌回 -2 不回 -14」/ octo-agent PR#1575 同错佐证;用户真机验证:微信上先发一句话刷新令牌即恢复)→ `send_item` 内建降级 = 带令牌撞 -2 去令牌原样重发一次(无令牌发送是 openclaw 合法路径),仍失败才退回并提示「让 TA 先发一句话」;顺带 ret/errcode **双字段判定**(此前只看 ret,errcode-only 失败被当成功误报「发出去了」)。3 个假端点测试锁行为,Mac 全绿 487+16 / clippy 触点零警;**真 iLink 对无令牌发送的接受度 = 真机 watch**(日志认「疑似 context_token 过期,去掉令牌重发一次」行)。规则变更 = §7.7 ② 过程级补记)。前次 2026-07-15·深夜(**动作确认闸**,§7.8 新★条 =「先能力后闸」的「后」、web_render 敢默认放开用的前提〔Mac 全绿 484+16 / clippy 触点零警 / vue-tsc / i18n 镜像 639 / 预览验过 HUD 卡+足迹分组 / 注入脚本浏览器冒烟三段全过;真站点撞闸/渠道回话/口头确认待真机真网〕:用户四轮拍板——①触发 = 高危词表 ∪ 模型自报(词表内容 §4.11 三张一次定:高危动作/渠道回话/语音肯否,单源 `confirm.rs`);②UI = HUD 任务卡同族 + 悬浮窗展开面板可点(否决聊天流内联卡);③渠道确认照 robot 形态推审批消息回发起 chat(文本回话代码判定,推翻我起初「渠道立即拒」;配套 TG/微信收循环 spawn 化对齐钉钉——内联 await 会卡死确认回话);④审计要等级不要(足迹页「确认过的操作」分组兑现当年预留;等级/总开关/per-host 免问全否,「谁有权点头」= 认会话不认人);⑤语音双通道(念问句 pushDelta 同管线 + `Phase::ConfirmListen` 听一轮,cpal 源不开听,「宁可不认」)。机制 = 壳层动作执行点两段式(resolve 活 DOM 文本 → core `risky_hit` 单源 → armed 真点;`confirmed+expect_text` 内部字段重发,注入教不动);准入判据「出圈且收不回」立为新工具自检项;`Tool::risk()` 仍不消费(§6.5/§9 陈说法已订正)。规则变更 = §7.8 新★条 + §6.5/§9 订正)。前次 2026-07-15·夜(**web_render 文件上传**,§7.8 defer 半边解除〔Mac 全绿 470+16 / clippy 触点零警 / vue-tsc / i18n 镜像 +1〈step.render_upload〉;**预览浏览器(Chromium≈WebView2)用真实生成脚本冒烟全过**,WKWebView 真窗/真站点待验〕:用户拍板「做了吧」→ `upload_ref`+`upload_paths` 把本机文件传给页面上传框——机制 = Rust 读文件 → base64 **分片 eval** 暂存(`window.__lwUpStage`)→ 输入脚本组装 File + DataTransfer 赋 `input.files` + 派发 input/change(Playwright 同思路纯 JS 版);快照认 `input[type=file]`(accept/multiple/**已选名回读**自校验)且**隐藏文件框豁免可见性过滤**(「好看按钮 + display:none input」常见页型),编号不是文件框就近找(自身→后代→label);新 `click_note` 通道把动作折扣如实说(单选框给多个只传第一个 / [N] 不是文件上传框);闸 = 总量 50MB 沿 web_download 口径(单源 `webrender::UPLOAD_MAX_BYTES`,§4.11 不另造默认);凭证代填/CDP 仍不做、动作级确认闸门仍另案。**冒烟手法记档**:壳层单测把 format! 生成的注入脚本原文倒出 → 预览浏览器逐段真跑(模板括号转义类 JS 语法错只有真跑才抓得到)——单选/多选/隐藏框/非文件框拒/分片字节完整/上传后快照回读全过。规则变更 = §7.8 过程级补记)。前次 2026-07-15(**丢图留话修 bug + read_image 拉取式看图原语 + vision 目录校订/override**〔Mac 全绿 468+16 / clippy 触点零警 / vue-tsc / i18n 镜像 613 / 预览验过「图片理解」下拉;真模型行为待验〕:起因 = 上批「接下来补什么能力」推荐里「视觉半边」落到用户拍板——「截图对非视觉主脑空转还带误导 = bug,修掉,模型知道自己能不能看;pdf_to_png 自动附图没必要;反而给『读文件』配一个『读图』,模型自己要看就调」。① **§6.3 丢图必留话**:四方言(openai_compat 协议恒丢 / anthropic・gemini・responses 非视觉时)共用单源 `llm::tool_result_text`,丢弃 ToolResult 图片必在结果文本追加「附带的 N 张图片没能传给当前模型…」——此前无声丢弃,web_render 文本却宣称「已附上截图」= 喂假前提;无图/已送达字节不变缓存零损伤;② **§7.8 新 `read_image`**:缺省「刚发的那批图」/收绝对路径、缩 1600 重编码(透明 PNG/无透明 JPEG)、≤5 张超额如实退回、批超封顶如实说明;pdf_to_png 描述加组合指路(转完接 read_image 看);**视觉子调用(非视觉主脑「代看」)不做** = §9 扫描件 OCR 边界对默认用户不动,视觉主脑则白捡「重看旧图」与「pdf_to_png→read_image 看扫描件」两条组合链;③ **同日追加(用户三连:400 还是占位?/设置要能标能力照 robot/目录信息陈了)= §6.3 三件补**:「服务商自己占位」不可赌(DeepSeek 两端点两行为)、`ModelOverride.vision` 三态 +「图片理解」下拉(robot supported_media 的对应物,per-model 而非 per-provider)、目录 2026-07 校订(Kimi K2.5/2.6 视觉 / Qwen vl・omni 家族 / DeepSeek API 仍无图——web 官方核实,截图是二手汇总不全信)。规则变更 = §6.3/§7.8 过程级补记)。前次 2026-07-13·夜(**从备份恢复**,一键备份的另一半〔Mac 全绿 452+16 / clippy 触点零警 / vue-tsc / i18n 镜像 618 / 预览 zh+en 双验;Windows 真机待验〕:起因 = 用户问「有什么能力缺口值得做」→ 按代码对账出推荐清单(视觉半边 / 视频消息听音轨 / 备份恢复 / web_render 填表…)→ 用户拍板先做恢复——一键备份只有导出、没有任何导入口,换机/重装/盘坏时普通人拿着 zip 没门。§6.2 新「从备份恢复」条:机制 = **暂存 + 重启时开库前落位**(运行中不能覆盖已打开的 DB)——「恢复…」选 zip → `restore_precheck`(结构/魔数/**迁移版本前向检查**,备份来自更新版本拒并指路升级)→ 内联确认 → `stage_restore` 解到 `restore-pending/`(zip-slip 防护、未知条目跳过=向前兼容)→ 重启;boot 在 `Store::open` 前 `apply_pending_restore`:现库连 -wal/-shm 挪成 `larkwing.db.pre-restore-<时间戳>` 保险副本 → 备份库就位 → 克隆音色合并(同名以备份为准),失败逆序回滚老数据仍权威;成败经 `data_location.restored` → boot toast 绝不静默(§3.5)。记档:Windows keyring 密钥不进备份、换机恢复后 LLM key 重填;顺手修 `backup_to`/`restore_precheck` 临时文件名秒级时间戳并发同名互删(各加进程内序号,测试并行实锤 flake)。**同夜小补:「存图到电脑」**(用户拍板「做吧」;此前文件可以图片不行——图片分支只当轮注入+缩略图、上下文里没路径)——图片对称落收件区原名件 + 「已存到本地」路径行进 history(§9 B 条「留后续」解除;attachments 缩略图/当轮注入均不变,代价 = 图双份存储;光秃名按 mime 补扩展;单测 +1,453+16 绿)。规则变更 = §6.2 过程级补记 + §9 图片收件区半边解除(§10 item 3))。前次 2026-07-13·晚(**web 长文续读 + PDF 直链指路**,调研链小增强〔Mac 全绿 448+16 / clippy 触点零警,前端零改;真模型行为待验〕:起因 = 用户拿 Obscura(Rust 无头浏览器)文章问路 → **评估否决**(非 Blink 自研 DOM+V8、问题域 = 并行抓取/反检测 vs 我们 = 用户差事 + 人机接力、headless 丢可见任务窗/窗 cookie 承重件;重评信号记 Claude 记忆)→ 顺势对账「搜资料做调研」链路发现两短板并当日修掉:① §7.4 补「长文可续读」——`web_fetch`/`fs_read_text` 加可选 `offset`(字符起点),截断话术直报「继续读带 offset=N」,全文本就在 10min 缓存(Page JSON)/源文件里,**不落盘不建新机制**(页内链接只随首段;`tools::arg_u64` 收字符串数字 quirk);② `web::non_page_hint` 拦 PDF/二进制直链指路 web_download→fs_read_text(只认明确二进制 CT/%PDF 魔数;web_search 正文富化同闸静默降级——原先把乱码当证据);③ **搜索源单点记档**(DDG 国内直连不通+默认代理关 = 无代理用户 Bing 单点,第三源真机撞上再议);web_render 截断/web_search 片段/「调研」任务工具刻意不动(§5)。规则变更仅 §7.4 过程级补记)。前次 2026-07-13·午(**渠道会话管理**,用户拍板三件〔Mac 全绿 443+16 / clippy 触点零警;三渠道真机/真网待验〕:① §7.7 新「渠道会话管理」条**取代**「回访同一 chat 恒续同一会话(映射到固定 conv_id)」——单聊 12h 闲置轮换(判据 = 会话 `updated_at` 任意角色;chat 的会话 = 名下**最近有动静**的那个,提醒刚到点的老会话被叫醒 → 用户回「收到」接回它有上文),群聊不轮换(TG `chat.type` / 钉钉 `conversationType` / 微信恒单聊,`drive_turn` 加 `single`);② 映射改**历史行**(迁移 0023 去 UNIQUE;改绑插新行继承指认/昵称/推送地址,老行留档 = `thread_by_conv` 反查不断链——提醒推手机/发起人标签对轮换走的老会话照样命中;`set_push_id`/`set_label` 按 (channel,ext_id) 全行保鲜、`bind_user` 追溯全行、`list` 每 chat 只出最新行);③ **映射悬空自愈**——桌面删渠道会话后那个手机 chat 原本永久报错无自愈(本日发现的 latent bug),现名下无活会话即重建重绑;解析收口 `channels/mod.rs::resolve_conv`(时间注入可测,阈值单源 `FRESH_CONV_IDLE_MS` §4.11)。前次 2026-07-11·晚(**v0.2.14 发版整理**:§7.6 窗控实现状态订正——分平台代码已写齐随版入库〔winmax.rs Windows 最大化 hack + tauri.macos.conf.json Mac 原生窗 + 影院全屏判据卡死修〕,整理时 grep 发现 `winmax::fix_maximize` 写了组件没接调用点、已补上(lib.rs setup 主窗 hwnd 后挂);两平台真机验仍全欠、CHANGELOG 加 0.2.14 节;无规则变更)。前次 2026-07-11·午(**微信渠道(腾讯官方 iLink bot)全接**,用户看到「微信 ClawBot」问「怎么支持」→ 探针实证非 OpenClaw 客户端可自接〔`iLink-App-Id: bot` 头即拿到二维码、无 per-app 密钥〕→ 用户拍板「全做」:① §7.7 翻「微信暂不做」🔒 边界(2026-06-17 拒因「无官方 API」前提已变——腾讯 WeixinTeam 的 iLink bot HTTP API 出站长轮询免公网、同构 Telegram;不装 npm 插件、`net::Client` 裸接协议,MIT 源码当规格);② 新 `channels/weixin.rs`——切片1 文本收发〔`getupdates` 长轮询游标 `get_updates_buf` + `sendmessage` + 头部 `iLink-App-Id`/`X-WECHAT-UIN`/`Bearer` + `context_token` 复用 `push_id` 列回显〔零迁移〕 + allowFrom〕、扫码登录〔`get_bot_qrcode`→轮询 `get_qrcode_status` wait/scaned/need_verifycode/confirmed + IDC 重定向,`qrcode` crate 渲 SVG,扫码人 `ilink_user_id` 自动进白名单〕、切片2 入站媒体〔`aes`+`ecb` AES-128-ECB PKCS7 解密 `full_url` → InAttachment,语音先用 `voice_item.text`、SILK 后置 watch-item〕、切片3 出站文件 send_file 微信臂〔`getUploadUrl`+md5/`hex`+random aeskey → AES 加密 PUT CDN → `x-encrypted-param` 回填 item〕;③ 接线镜像 TG/钉钉:`mod.rs` mod+开关 spawn+outbound push 臂、`secrets` 加 `remote.weixin.token`、`remote_status` 加行、设置·远程加卡+i18n zh/en、新非秘密键 `remote.weixin.{allowed_users,base_url}`;④ 协议常量〔base/cdn url、`bot_type=3`〕是协议事实非 §4.11 产品默认,单源 weixin.rs 顶部;⑤ 账号风险如实记档〔官方渠道远比 itchat 安全但个人微信绑 bot 残余风险非零、借 OpenClaw 身份壳〕。Mac 全绿 + 单测 + 预览;真微信扫码/收发/媒体 = Windows/真网/真号 watch-item〔PLAN〕)。前次 2026-07-10·夜(**唤醒即时反馈音「叮」+ 修唤醒在听时「停/定稿」键失效**,用户把「删唤醒确认层?」议题拨成「不删、旁听保留 + 唤醒不延迟」〔Mac 全绿 415+16 / clippy 触点文件零警 / 加 chirp 单测〕:① §8.2 ★ 增两条——命中候选那一刻 `prompts::play_chirp_async` core cpal 直出一声「叮」(**纯正弦短音**:silero VAD 不当人声、ASR 不转字、浏览器采集 AEC3 直接消 → 不污染静默续录/孤立呼名判定;`CHIRP_*` 单源可调 §4.11),把「反馈」与「判定」拆开 = 治「很多时候不知道有没有唤醒」而**不删确认层**(旁听/一句话直达/防误应全留),唯一代价=误命中也「叮」一下(默认名 BT 非常用词→极少);② 修「在听时点停没反应」——唤醒录音跑自己的循环线程、不占听写会话槽,`listen_stop` 此前够不着 → 新增 `Inner.wake_ctl`(唤醒侧开录前 `arm_wake_ctl`、`collect_utterance` 读;`listen_stop` 同写会话槽与 wake_ctl,二者互斥),✕=安静回待唤醒、定稿键=发已听到的,前端零改;③ 同夜追加:**媒体在播跟进窗 6s→3s**(用户拍板「暂定 3s」,`FollowUp{media_playing}` 前端念完那一刻传 hint,§7.5 新条)+ ④ **修「媒体音量隔好几分钟才恢复」双病灶**(真机实锤:useChat 三条早退路漏发唤醒收尾通知 → core 卡 `AWAIT_TURN_CAP=180s` 兜底;core Resume 静默回 Watch 不发 ListenEnded → 失败/取消回合 duck 永不恢复;两处已修 + §7.5 立「唤醒终态分支必带 wake settle」规则);真麦体感/召回待 Windows·真机)。前次 2026-07-10·晚(**轻办公原语一批:qr_decode / web_download / pdf_to_png / send_file(+web_fetch 页内链接)**,用户拍板「发票链场景 + 分段能力让 agent 组合」〔Mac 全绿 407+16 / clippy / vue-tsc / i18n 镜像 577;pdfium 真网 e2e 已验;TG 真 bot/真钉钉/Windows 待真机〕:① §7.4 web 二件套 → **三件套**(web_fetch 带页内链接挑「下载」按钮、web_download 落盘 50MB 闸永不覆盖);② 新 §7.8 轻办公原语——纪律 = 全正交原语绝不造发票任务工具,发票流程 = 五原语自由组合(§5 试金石实弹);qr_decode 缺省「刚发的那批图」(`recent_image_attachments` 一批语义);pdf_to_png = **pdfium 组件**(components 第三成员、tgz 新形态、**pdfium-render 绑定每进程一次** OnceLock 缓存,顺手备好 §9 扫描件栅格化半边);③ §7.7 出站文件 send_file——目标恒为说话人(归人映射反查),TG sendDocument multipart / 钉钉新旧 token 混用 media/upload+batchSend、图片走 sampleImageMsg;PDF 合并/抽页按用户拍板**不做**(发票单独上传用不上);④ 同夜真机首测连出两修——qr 解码器 rqrr→rxing+Otsu 阶梯(§7.8「真拍照验」教训)+ **web_render 真浏览器档**(真票据平台是 SPA 撞出的缺口,用户拍板两切片一起:隐藏 WebView 窗渲染 + click_text 点击接下载,§7.8 新条;真机端到端验通=真票据平台点「下载」出 PDF);⑤ 同夜三次拍板——**浏览器操作路线 = DOM/文本编号快照主干、混合留口、CDP/纯视觉否**(§7.8 路线拍板段)+ **L2 会话式浏览落地**(session 窗 TTL 180s/最多2/清扫任务、`click_ref` 按编号点、`back` 返回、点击等待分寸三档;输入/填表仍不做等 risk 闸门;截图混合档经核实 `ToolResult` 仍纯文本 → 明确 defer;Mac 全绿 414+16 / clippy 零错;会话窗真机待验);⑤′ **AEC 试验块退役**(采集源开关挪正式位=声音 tab「回声消除」开/关挨着麦选择器,AecSpike+aec.* 文案删,NS/AGC 仍锁死不暴露,§7.5 更新);⑥ **可见任务窗四批落地**(渲染窗右下缩略直播 set_zoom 联动/focused(false)/skip_taskbar、窗题跟页面标题、关窗=收会话、HUD 任务卡 kind=webrender 四步进,§7.8 末条——登录墙人机接力由此解锁;Mac 全绿 415+16 / clippy / i18n 589;缩略窗观感与 Win 行为待真机)。前次 2026-07-10·早(**唤醒词 = 名字派生 + 默认名 7274→BT**,两条 🔒 变更均用户当面拍板〔Mac 全绿 415 / clippy / vue-tsc / 预览验过;真麦召回待 Windows/真机〕:① §8.2 新★条**取代**「默认唤醒词=小七」——用户点破「起名叫 A、喊 B 才应、还自称 A = 糟糕设计」,唤醒词不再是独立设置(输入框已删、`voice.wake.keywords` 白名单/写入臂退役成死数据),单源 = `ui.pet_name` 现派生:中文原样/数字转读法/**字母缩写按中国人的字母读音**(BT→逼踢,`wake::LETTER_READINGS`),英文单词名派生不出 → 回落默认词组 + `wake_fallback` 如实提示;**确认层 `to_syllables` 与派生共用同一张读音表**(ASR 吐拉丁原文「BT」才对得上,否则真呼叫被当幻听);② §4.1 默认名改 **BT**(BT-7274 小名),默认唤醒词组「逼踢+七二七四」单源 `wake::DEFAULT_WAKE_WORDS`,「小七」退役;§4.11 双源清单同步;顺手修:consolidate 转录 assistant 标签硬编「7274」→ 中立「助手」、index.html 静态 title 用产品名 Larkwing、上批遗留的 care-tone 问号机测全半角重复(clippy eq_op,全角改 `\u{FF1F}` 同 judge-diary 修法)。前次 2026-07-08(**手机端体验二期 + 关怀候选扩待办 + eval LLM-judge**,四件一批〔Mac 全绿 383+16 / vue-tsc / 悬浮窗预览验过;渠道两件真机/真网待验〕:① §7.7④ 文档半边解除——TG document / 钉钉 file 能抽文字的文档与「以文件发送」原图走桌面同缝 InAttachment〔`attach::doc_supported` 下载前预检 + `image_mime_by_ext`〕,压缩包/视频仍如实回「看不了」;② §7.7 出站富格式解除「以后」——`channels/render.rs`〔pulldown-cmark〕TG 转 HTML parse_mode〔源片切→逐片转→超限/4xx 降级纯文本重发〕、钉钉 msgtype markdown,`looks_markdown` 保守探测、纯文本零处理,绝不走 MarkdownV2;③ 悬浮窗关怀候选多一种 kind=todo〔`FloatIdle.care`→`cares` Vec,拖最久的 open 待办「还记着:X」,`TodoRepo::oldest_open`;点击替用户发「帮我看看…怎么办」同 care-go 链〕= 切片2 小账的 L0 半边;④ §16.3 LLM-judge 首版——场景级 `.judge(rubric)`+`run_scenario_judged`,判官=specs 最强档全程固定〔列间可比〕,判官故障计 judge_errors 不算 fail〔评审挂了≠被测差〕,新场景 judge-care-tone〔CARE_FOLLOWUP 语气半边〕/judge-voice-style〔说话守则,新 `.say_spoken`〕,判官自测 FakeLlm 4 测。规则变更仅 §7.7 两处「留后续」随既成事实解除)。前次 2026-07-07(**§9 PDF 边界订正 + 对账回写**:一次「PLAN/AGENT 说的 vs 代码真相」对账发现文档落后于代码——§9「PDF 暂不做 / `extract_doc_text` 对 PDF 返回 None」是陈状态,实际 0.2.0 头牌①已用 `pdf-extract` 做了 **PDF 文字层**〔`attach.rs::extract_pdf`,2026-06-30 Windows 验过真中文 PDF〕,故 §9 从「暂不做」改为「文字层已做、扫描件 OCR/栅格化仍 stretch 未做」〔§10 item 3 解半边边界〕。同批 **PLAN 侧**订正两处陈状态〔音色克隆现场录音入口「留后续」→ 已落地、电源类桌面控制「缓做/直交原语两个」→ 已在 0.2.0 补齐 `power` 第三原语并真机验过〕+ eval 加「待办小账·记/结分寸」反例守 3 场景〔`care-no-forced-todo`/`care-notes-open-loop`/`care-no-premature-finish`,真模型 pass^k 待跑〕。教训:文档自述状态会漂,决定「接下来做啥」前先按代码对账。规则真相源无实质变更〔仅 §9 半边边界随既成事实解除〕)。前次 2026-07-06(**唤醒确认层 + AEC 解锁**:高频名「天天」误唤醒治理两层落地——层2 命中三段式〔KWS 候选 → 静默续录 → ASR 拼音对齐 → 孤立呼名/旁听仲裁/幻听拒〕+〔旁听〕临时回合〔`__IGNORE__` 蒸发零痕迹 / 开口转正,`send_overheard`〕,§8.2 新★条;层1 AEC 从「明确不做」解锁为「采集端浏览器 AEC」路线〔WebView2=Chromium AEC3,参考=自播全部音频〕,spike 块进设置·声音、力度数字待 Windows 真机,§7.5 改写 + §9 移出;Mac 全绿 365+19、eval 加 overheard 双场景)。前次 2026-07-04·夜(**去助手名字硬编化**:用户可见文案里凡露出助手名字〔11 条 zh 文案 + en 镜像 + Memory/Reminders/Ops mono 徽章 + telegram onboarding〕全改 `{name}` 占位,前端由 `petName` / core 侧由 `Engine::pet_name()` 注入,名字 = 用户数据不进 i18n;新增 §6.6「用户可见文案绝不硬编助手名字」用户准则 + §4.1 交叉引用。缘由:「旺财」是暖萌皮名违 §5 中立、且用户可随时改名 → 硬编必漂移)。前次 2026-07-04·晚(v0.2.6 发布:**播放治本** = 本地不兼容 BMFF 片走「音视频分离自适应」——视频 `-c:v copy` 省 CPU(关键帧对齐变长段)+ 音频**离散段 + appendWindow 裁 priming** 治「音画越放越快」漂移,手写 MSE 绕开 shaka,双层兜底回落 muxed;§7.1 大改。两轮真机迭代踩到「Mac 预览 Chromium 能放、WebView2 不行」两坑〔整片音频流式爆配额→驱逐开头 t=0 无声;WebView2 fetch 不吐流式 body→abuf 空〕,靠诊断日志桥 `media_log` 定位,§8.1 记。同版:声纹归人〔另一会话,§4.2/§9〕+ 响度 −3dB。前次 2026-07-03·晚(v0.2.4 批次二:§7.7 手机端补全=提醒推回手机/TG 语音照片能收/看不了必回话、原「MVP 纯文本」入站半边解除,channels 组合 voice+media;§7.1 加「视频已兼容」整文件 copy-remux 缓存;§7.6 重试载体补语音模型三型。同日早:多用户第一步「渠道归人」启用 = 家人页激活 + 渠道对话指认给家人 + speaker_user 归人全链,§4.2/§7.7/§9 三处更新;声纹仍休眠为第二步。前次 2026-06-30:模型「高级设置」override 落地=按模型纠正档位/上下文/价/计价方式,catalog 进程级 overlay;计价感知压缩补上=无缓存少留;窗口 UI 以 K 计。前次 2026-06-29:§9 收窄媒体规则=文档文字进 history、图仍当轮;上下文处理合并成单一 model-aware 字数预算 + 整块锚定、删条数窗口 WINDOW_MAX;catalog 加 ctx_window_tokens。2026-06-20 新增 §4.11 用户准则)。原合并自 CLAUDE.md(宪法)、PLAN.md(§0–§12 设计与执行状态)及历次会话的踩坑沉淀。*

## 分家前 §0「现状」段(2026-07-12 快照)

> 来源:分家前 AGENT.md「0. 一分钟认识 Larkwing」L30。

- **现状(最新 v0.2.17,2026-07-12 真机清账)**:MVP 后端 + agent/工具运行时 + 影音 + 任务需知 + 文件能力 + 提醒/web + 语音 + 常驻临场(开机自启/托盘/悬浮窗)+ 远程渠道(TG/钉钉/**微信 = 腾讯 iLink bot,多绑定「一人一 bot、家里人各自扫码」**,图/语音/文档全量)+ 多用户(渠道归人 + 声纹归人)+ 主动关怀 + 说话人显性化 + 唤醒确认层 + 命中即人声应答 + AEC 采集 + 硬件加速转码 + 轻办公原语(二维码/网页会话浏览/PDF转图/发文件)+ 人际路由(跨人提醒/捎话/发文件)+ 手机发文件落收件区「存到电脑」+ 无文字附件攒批 + 窗控平台分叉**均已落地**,核心测试全绿(437+16)。**✅ v0.2.7→v0.2.17 这批真机欠账 2026-07-12 用户已一轮验清**(0.2.14 总验收单 53 项 + 0.2.15→0.2.17 增量:唤醒命中即应答/改名即生效/开机聋根治、微信多绑定双号在线、发文件存电脑闭环),**仅剩两项长期观察**(家庭日记文笔 · 微信平台风险,给不了一次性结论、不占欠账)。**权威执行状态 + 真机验收勾项以 PLAN.md 各 § 为准(见「2026-07-12 真机验收纪要」)。**

